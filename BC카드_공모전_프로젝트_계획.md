# 제1회 AI금융빅데이터플랫폼 소비데이터 공모전 — 프로젝트 계획

> 2026-09-22 갱신. 처음 계획에서 **확정·변경된 내용과 진행 상태**를 반영했다. 방법·검증·정정 이력의 전부는 [CHANGELOG.md](CHANGELOG.md), 재현 방법은 [README.md](README.md).
> 결과물: 웹사이트 https://ahnseo0000.github.io/BC_card/ (`docs/index.html`)

## 주제
**"소비는 있는데 왜 못 버티는가 — 설명 가능한 상권 생존 모델"**

BC카드 소비데이터로 상권의 수요 구조를 읽고, 그 상권의 실제 생존율(개폐업)이 왜 갈리는지 통계적으로 설명 가능하게 분해하는 모델.

BC카드 기존 서비스(상권지수 — 스냅샷 등급, Biz Credit — 개별 가맹점 휴폐업 예측)와 다르게, **"지역×업종 단위의 생존 구조를 요인별로 분해"**하는 진단 레이어를 채우는 포지셔닝.

> **표현 원칙(확정)**: 이 프로젝트가 보여 주는 것은 통계적 **연관**이다. 계획 초안의 "원인 분해"는 "요인 분해(연관)"로 쓰고, 발표·화면·문서에서 인과나 정책 효과로 읽히지 않게 한다.

---

## 진행 현황

| 단계 | 상태 | 결과 |
|---|---|---|
| 0. 안전판 | 완료(대체) | `cox_rsf.py --biz` 소규모 스모크 실행으로 파이프라인 검증 |
| 1. 데이터 정합 | 완료 | 전 컬럼 정제, 업종 9개 통합·모형 7개, 시군구 키 기준표, 프랜차이즈 목록(수작업), 조인 99.99% |
| 2. Kaplan-Meier | 완료 | 지역×업종 1차 스크리닝, 대형할인점·슈퍼마켓은 KM만 |
| 3. Cox / RSF | 완료(Cox) · 진행(RSF) | Cox 메인 **M3L**. RSF는 팀원 환경에서 비교 중 |
| 4. 요인 분해(SHAP) | 완료 | Cox 선형예측자의 정확한 분해 + 블록 제거 예측 중요도 |
| 5. 공간 확장 | 완료(대체) | 이웃 시군구 폐업 이력(공간 지연)으로 잔차 공간 군집 해소 → GNN 미진행 |
| 추가 검증 | 완료 | 교란 점검, 그룹·시군구 5-fold, 소비·경쟁밀도 추가 지표 검증, 전향(시간 밖) 검증 |
| 기획·제품화 | 완료 | 웹사이트(지도 탐색·상권 분석·모형 근거), 상권 유형, 내 점포 진단 |

---

## 데이터 구성 (역할 고정)

| 데이터 | 성격 | 역할 |
|---|---|---|
| BC카드 소비데이터 (기본제공, 242,574행) | 시군구×업종×성별×연령×월 집계 (2026.1~6월) | 수요측 공변량 — 지역×업종별 성별·연령 구성, 소비 규모·추이 |
| 지방행정 인허가데이터 LOCALDATA (data.go.kr, 무료) | 개별 사업장 단위, 인허가일자·폐업일자·영업상태 | 생존분석 관측단위(결과 변수) |
| 소진공 상가(상권)정보 (data.go.kr, 무료) | 6월 스냅샷(폐업 점포 누락) | 경쟁밀도 후보로 검토했으나 **최종 모형에서는 쓰지 않음** — 6월 스냅샷이라 1/1 기준 밀도를 만들 수 없고, LOCALDATA 1/1 점포 수로 대체한 경쟁밀도도 예측 개선이 없었다 |

**주의**: BC카드 데이터는 개별 사업장 식별자가 없는 집계치이므로 생존분석의 관측단위가 될 수 없음. 생존분석은 LOCALDATA에서만, BC카드는 그 생존모델의 설명변수로만 사용(시군구×업종 단위의 생태학적 변수).

---

## 분석 파이프라인

### 0단계 — 안전판 (완료)
표본 큰 업종 1개로 조인→전처리→생존모델 전체 흐름을 먼저 검증. 별도 스크립트(`safety_net.py`)는 삭제하고 `cox_rsf.py --biz 편의점` 스모크 실행이 대신한다.

### 1단계 — 데이터 정합 (완료)
- LOCALDATA 업종 ↔ BC카드 업종 매핑. 데이터에는 9개 통합업종, **생존모델은 7개 업종**(한식계열·일식회집·중국음식·서양음식·스낵·제과점·편의점). 대형할인점·슈퍼마켓은 표본이 적어 KM으로만 본다.
- LOCALDATA 도로명·지번 주소 → 시군구 키(신설 구·통합시 개편 반영, 인천 구 크로스워크), 지역키 기준표(`data/region_master.csv`).
- BC카드 결측 조합은 "0"이 아니라 "비공개(소규모셀 마스킹)"로 처리, 마스킹 행은 구성비 분모에서 제외.
- 좌표 이상치는 삭제하지 않고 플래그, 프랜차이즈는 브랜드 키워드 목록(`franchise_brands.csv`)으로 판별.

### 2단계 — Kaplan-Meier (1차 스크리닝, 완료)
지역×업종별 생존곡선으로 어디서 어떤 업종이 유독 빨리 폐업하는지 1차 발견하고 후보 변수를 스크리닝.

### 3단계 — Cox PH / Random Survival Forest (완료 · RSF 진행)
- **시간 설계(확정)**: 2026-01-01에 영업 중인 점포를 180일 추적하는 **단순 코호트가 주 설계**, 팀원의 landmark(3/31·6/30, 78일)는 강건성 검증으로 유지. 결과 변수는 폐업(event), 그 외는 censoring.
- **메인 모형(확정): Cox M3L** — 점포 변수(영업연수·프랜차이즈·입지·규모 등) + 그룹 직전 1년 폐업률 + 자기·이웃 시군구 폐업 이력(1/1 이전 정보만) + BC 성별·연령 구성 + 업종. 선형이라 요인별 기여를 정확히 분해할 수 있어 설명 목적에 맞는다. RSF는 비선형 성능 비교용(팀원, scikit-survival 환경).
- 소규모 표본 대응: 최소 점포 수 기준(조합 100개 이상 평가) + 부분 풀링(EB)은 민감도 분석으로 보관. 그룹 단위 변수라 유효 표본이 약 1,800개임을 반영해 **시군구 단위 부트스트랩**으로 불확실성을 본다.
- 경쟁밀도·소비 규모·객단가·성장률 등 추가 지표를 같은 틀로 검증했으나 예측 순위의 추가 개선은 확인되지 않아 최종 모형에는 넣지 않았다(CHANGELOG ⑲·㉑).

### 4단계 — 요인 분해 (독창성 핵심, 완료)
- Cox 선형예측자를 요인 블록별로 정확히 분해(오차 10⁻¹⁵): "이 상권·업종이 평균보다 몇 배 위험하고, 영업연수 ×a · 프랜차이즈 ×b · 지역 폐업 흐름 ×c ..."의 곱으로 표현.
- SHAP 크기는 변수 겹침으로 부풀 수 있어 **블록 제거 예측 중요도**(블록을 빼고 다시 학습한 5-fold 검증)로 교차 확인한다.
- 이를 바탕으로 지역×업종을 5개 **상권 유형**으로 묶고, 점포 조건을 넣어 보는 **내 점포 진단**을 제공.

### 5단계 — 공간 확장 (대체 완료)
모형 잔차의 공간 자기상관(Moran's I)이 남아 있어(0.43 → 0.16) 이웃 시군구의 1/1 이전 폐업 이력을 변수로 넣었더니 유의성이 사라졌다(0.02). 설명가능성과 충돌하고 노드 수가 작은 GNN은 진행하지 않기로 확정.

### 추가 검증 (완료)
- **교란 점검**: BC 연령 구성이 상권 유형(지역 유형)의 대리 변수인지 층화·통제 검정.
- **그룹·시군구 5-fold**: 미학습 그룹·미학습 시군구 순위 검증(평가 조합 1,090개).
- **전향(시간 밖) 검증**: 1~6월로 학습한 모형이 6/30 영업 점포를 9/16까지 78일 추적한 실제 폐업을 얼마나 맞히는가(C-index 0.627, 위험 최상위 10% 점포는 평균의 2.0배).

---

## 기획 파트 (실현가능성 20%, 비중 작게)
**타겟 오디언스(확정): BC카드 가맹점전략팀** — 가맹점·상권 리스크를 지역×업종 단위로 진단하고 우선 살펴볼 곳을 정하는 실무 관점.

결과를 **웹사이트**로 제공한다(GitHub Pages, 서버 없는 정적 페이지).
- **지도 탐색**: 시군구 버블 지도(상대 폐업 위험 / 실제 폐업률 / 상권 유형), 업종 필터, 검색, 지역·업종별 "왜" 요인 분해, BC 소비 구성, 내 점포 진단
- **상권 분석**: 시군구 프로필, 두 지역 비교, 위험·안전 순위, 상권 유형
- **모형 근거**: 요인 중요도, 검증, 전향 검증, 시사점, 데이터와 한계
- 챗봇은 LLM 대신 규칙 기반 설명·검색으로 대체(값이 지어질 위험이 없고 서버·키가 필요 없음). LLM 확장은 로드맵으로만 제시.

---

## 차별화 근거 (독창성 40% 방어 논리)
- BC카드 상권지수·Biz Credit = 스냅샷 / 개별 가맹점 예측(prediction)
- 이 모델 = 지역×업종 단위로 **왜 갈리는지 요인 분해(explanation)**, 방법론도 생존분석 + 정확한 분해 + 다층 검증(그룹·시군구·시간 밖)으로 상이
- 검증에서 확인된 한계까지 그대로 보여 주는 설명 가능성(음의 결과 포함 공개)

---

## 한계 정리

### 구조적 한계 (기법으로 해결 불가 — 명시만)
- **연관 ≠ 인과**: 요인 배수와 순위 개선은 관찰된 연관이며 정책·개입 효과가 아니다.
- **폐업≠실패**: 이전·업종전환·직권 처리가 "폐업"으로 뭉뚱그려질 수 있음 → "해당 사업장·해당 업종으로서의 지속"으로 정의 범위를 좁혀 방어.
- **BC카드 데이터의 해상도**: 시군구×업종 평균이라 점포 매출이 아니다. 점포 단위 결과와 해상도가 달라 소비 지표만으로는 위험을 가르는 데 한계가 있다(점포별 매출이 있으면 재검증 가능).
- **관측 창**: 2026-01~06 BC 집계와 6개월 추적(전향 검증 78일)이라 계절·경기 순환을 다루지 못한다.

### 부분적으로 완화 (실제 적용)
- **소규모 표본 불안정** → 최소 점포 수 기준, 시군구 단위 부트스트랩, 표본 30개 미만 조합은 화면에서 구분 표시.
- **LOCALDATA 사업자번호 부재** → 상호·주소·업종 기반 매핑, 이상 케이스는 플래그로 남기고 비중을 CHANGELOG에 투명 보고.
- **주소단위 집계 손실** → 생존분석(개폐업 이벤트)은 LOCALDATA 점포 단위로 유지, BC카드 공변량만 시군구 단위로 조인해 손실을 공변량 쪽에 국한.
- **프랜차이즈 판별** → 수작업 브랜드 목록이라 누락이 있을 수 있어 민감도 확인을 남은 일로 둔다.

---

## 결정 사항 (확정)
- [x] 메인 모델: **Cox PH (M3L)**, RSF는 비선형 비교용으로 병행
- [x] 5단계: 공간 지연 변수로 대체, **GNN 진행하지 않음**
- [x] 기획 타겟: **BC카드 가맹점전략팀**
- [x] 시간 설계: 단순 코호트 주설계 + landmark 강건성 검증
- [x] 업종 범위: 데이터 9개, 생존모델 7개
- [x] 산출물 형태: 정적 웹사이트(지도·분석·모형 근거), 챗봇은 규칙 기반

## 남은 일
- [ ] RSF에 최종 모형(M3L) 변수 반영 후 재실행(팀원)
- [ ] BC 원자료 연령·성별 코드 정의 확인 → 사이트 라벨 갱신
- [ ] 인천 서구·중구 입지 이상 원인 확인, 프랜차이즈 목록 민감도
- [ ] 발표 자료·설명 문장 정리
- [ ] (선택) 위험 곡선·집중도, 관측 기간 연장 강건성, 배수의 불확실성 구간
