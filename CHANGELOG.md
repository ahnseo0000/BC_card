# CHANGELOG — 데이터 전처리 / 조인 작업 기록

## 개요

`data/ABP_CONTEST_DATA.csv`(대회 필수 제공 BC카드 데이터, 이하 ABP)와 직접 수집한 LOCALDATA 4종
(`식품_일반음식점.csv`, `식품_제과점영업.csv`, `식품_휴게음식점.csv`, `생활_대규모점포.csv`)을
BC카드 11개 업종 기준으로 매핑·정제하고, `(SIDO_NM, CCG_NM, bc_업종)` 키로 조인해
생존분석용 통합 테이블(`data/final_joined.csv`)을 만드는 작업.

관련 스크립트 3개, 실행 순서대로:
1. `preprocess_localdata.py` — LOCALDATA 4종 정제
2. `preprocess_bc.py` — ABP 정제
3. `join_datasets.py` — 두 결과 조인


---

## 발견된 문제와 수정 내역

### ① 주소 파싱 우선순위 버그
- **증상**: `지번주소`가 값이 있기만 하면 무조건 채택했는데, 일부 행은 지번주소가
  `"68-5 68동 8호"`처럼 시도명 없이 잘려 있어 `SIDO_NM="68-5"` 같은 쓰레기값 발생.
  같은 행의 `도로명주소`엔 `"부산광역시 해운대구..."`로 온전한 주소가 있었음에도 무시됨.
  영향: 1,393행(0.065%).
- **수정**: `preprocess_localdata.py`의 `parse_area()` / `get_area_row()` — 시도명이
  `VALID_SIDO` 목록에 실제로 속하는지 검증한 뒤 채택하도록 변경. 지번주소가 유효하지
  않으면 도로명주소로 폴백.
- **결과**: 파싱 실패 1,393행 → 35행(0.001%대)로 감소.

### ② "전남광주통합특별시" 병합 지역명
- **증상**: LOCALDATA 원본 주소에 `"전남광주통합특별시 목포시 ..."`처럼 옛 전라남도+광주광역시가
  하나의 시도명으로 합쳐진 표기가 존재(130,265행). ABP는 `전라남도`/`광주광역시`로 분리되어
  있어 이대로 조인하면 전체의 약 5%가 매칭 실패.
- **원인 확인**: 하위 CCG_NM을 까본 결과 광산구·서구·북구·동구·남구(옛 광주광역시 5개 구)와
  목포시·여수시·순천시 등(옛 전라남도 시/군)이 섞여 있음을 확인.
- **수정**: `fix_region()` — `GWANGJU_GU` 5개 구 집합 기준으로 광주/전남 분리.

### ③ 일반구를 가진 시(市)에서 구(區)가 통째로 누락
- **증상**: 주소를 공백 기준 앞 2토큰만 사용해서, `"경기도 수원시 영통구 ..."`처럼 3토큰
  주소에서 구가 잘려나가 `CCG_NM="수원시"`로만 저장됨. ABP는 `"수원시 영통구"`처럼 구까지
  포함. 창원·청주·수원·성남·용인·천안·전주·고양·안산·안양·부천·포항·화성(4구) 등 영향,
  합산 30만 행 이상(전체의 15%+) — 발견된 문제 중 영향 범위가 가장 컸음.
- **수정**: `parse_area()`에 `GU_PATTERN`(`^[가-힣]{1,5}구$`) 검사 추가 — 3번째 토큰이
  구 패턴이면 CCG_NM에 합쳐서 `"수원시 영통구"` 형태로 저장.

### ④ ABP(BC카드) 데이터가 전혀 전처리되어 있지 않았음
- **증상**: `TP_BUZ_NM`에 `"편 의 점"`, `"슈퍼 마켓"`, `"제 과 점"` 등 공백 혼입. 또한 ABP는
  11개 업종(일반한식/갈비전문점/한정식 분리)인데 LOCALDATA는 이미 "한식계열"로 통합돼
  있어 조인 키가 안 맞음.
- **수정**: `preprocess_bc.py` 신규 작성 — 공백 제거 후 `TP_BUZ_MAP`으로 9개 통합업종
  (한식계열/일식회집/중국음식/서양음식/스넥/제과점/편의점/대형할인점/슈퍼마켓)에 매핑.
  미매핑 0건 확인.

### ⑤ 인천 신설구 · 세종시 표기 차이 (조인 후 QA에서 추가 발견)
- **증상**: LOCALDATA는 최신 행정구역(검단구·영종구·제물포구·서해구 등 신설구)을 쓰는데,
  ABP는 2026년 상반기 스냅샷 시점 기준 옛 구명(서구·중구·미추홀구)을 씀. 세종시는 ABP가
  구분 없이 `SIDO_NM`="세종특별자치시" 하나로만 주는데 LOCALDATA는 조치원읍·나성동 등
  읍면동까지 내려가 있었음.
- **원칙**: ABP는 대회 [필수] 제공 데이터이자 고정된 시점 스냅샷이므로, [선택] 외부
  데이터인 LOCALDATA를 ABP 기준에 맞춘다 (역방향 아님).
- **수정**: `INCHEON_CROSSWALK`(검단구→서구, 서해구→서구, 영종구→중구, 제물포구→미추홀구),
  세종시는 CCG_NM을 SIDO_NM과 동일하게 강제.
  ※ 인천 크로스워크는 공개된 개편 계획 기준 추정 — 팀 내 교차확인 권장.

### 그 외 정합성 체크 (버그는 아니었음, 확인만)
- 폐업일자 < 인허가일자(논리 오류) 건수: **0건**.
- 인허가일자 1950년 이전 극단치: 93행 제거.
- `영업상태명` 분포 확인 — `event_observed`는 `'폐업'`만 1, `영업/정상`·`휴업`·
  `취소/말소/만료/정지/중지`는 모두 censored(0) 처리. (휴업/취소류를 다르게 다룰지는
  아직 팀 논의 필요 — 미결정 사항 참고)
- ABP `GENDER_CD`/`AGE_CD`에 `'x'`/`'x'` 조합(13,161행, 5.4%) 존재 — 소규모셀 마스킹으로
  성별·연령 breakdown이 비공개 처리된 행. **금액 합계(`bc_amt_total`)에는 포함**하되,
  성별/연령 소비 구성비 계산에서는 제외 처리.

---

## 조인 결과 (매칭률 추이)

| 시점 | 매칭률 | 비고 |
|---|---|---|
| ①~④ 수정 후, 지역명 크로스워크 전 | 98.24% | 미매칭 37,754행 |
| ⑤ 인천·세종 크로스워크 추가 후 | **99.93%** | 미매칭 1,583행 |

### 잔여 미매칭 1,583행 (0.07%) — 조치하지 않고 남겨둔 이유
- 화성시(81행): 원본 주소 자체에 구 정보가 없는 극소수 케이스. ABP는 이미 화성시를
  만세구/동탄구/병점구/효행구 4개 구로 세분화해서 제공 중이며, 해당 로직은 정상 작동
  중(2.6만 행 정상 매칭) — 이 81행만 원본 주소 결측.
- 연기군(26행)·여주군(1행): 세종시 편입·여주시 승격 이전의 옛 군명이 남은 오래된
  등록 주소 잔재로 추정.
- 봉화군·청송군·군위군의 일식회집(총 68행): 해당 시군구×업종 조합 자체가 ABP에
  존재하지 않음(진짜 데이터 없음일 가능성 — 버그 아님).

---

## ⑥ 시점 불일치 — BC카드(2026 상반기 스냅샷) vs LOCALDATA(수십 년치 이력)

- **문제**: ABP는 2026-01~06 딱 6개월치 스냅샷인데, LOCALDATA는 1970년대~2026년까지 걸쳐
  있는 폐업 이력. 조인 시 BC카드 쪽을 지역×업종 단위로 합계 내서 "2026 상반기 소비 패턴
  하나"를 만든 뒤 LOCALDATA 모든 행에 동일하게 붙였는데, 이러면 예: 1994년에 망한 가게에
  2026년 소비 패턴이 폐업 "원인"으로 붙는 시간 역전 문제 발생.
- **실측**: 전체 214만 행 중 지금(2026년)도 영업 중인 건 29.6%(635,315행)뿐이고, 폐업
  시점은 수십 년에 걸쳐 분산 — 2026년 상반기 중 실제로 폐업한 건 1.3%(28,939행)뿐.
- **조치**: 행을 삭제하지 않고 `final_joined.csv`에 `in_bc_window` 불리언 컬럼 추가
  (`join_datasets.py` 3b단계). 정의: `인허가일자 <= 2026-06-30` AND
  (`폐업일자` 결측(=아직 영업중) OR `폐업일자 >= 2026-01-01`) — 즉 2026년 상반기와
  존속기간이 겹치는 사업장만 True.
  - `in_bc_window=True`: **664,810행(31.0%)** — BC카드 공변량을 쓰는 3단계 Cox/RSF는
    반드시 이 필터를 적용해서 사용.
  - `in_bc_window=False`: 1,480,306행(69.0%) — BC카드 공변량 없이, 순수 LOCALDATA
    기반 장기 생존 트렌드(KM 곡선 등)에만 사용. 삭제하지 않고 남겨둔 이유는 이 용도로는
    여전히 유효한 데이터이기 때문.

---

## ⑦ 제물포구 크로스워크 정정 (팀원 `step5-group-time-split` 브랜치 검증에서 발견)

- **문제**: ⑤에서 `INCHEON_CROSSWALK`에 `'제물포구': '미추홀구'`로 넣었던 게 틀림.
  제물포구는 2026-07-01에 **(구)중구+(구)동구가 통합**되어 신설된 구로, 미추홀구와는
  무관함. 잘못된 매핑이어도 조인은 "매칭"됐었다(제물포구 사업장이 미추홀구 키로
  재라벨링되어 실제 미추홀구 소비 데이터를 붙여 받았음) — 매칭률에는 안 잡히고 데이터
  내용만 틀린 조용한 오류였다.
- **수정**: `preprocess_localdata.py`에서 `'제물포구': '미추홀구'` 제거(제물포구는
  LOCALDATA에 그대로 남김). `join_datasets.py`에 1b단계 추가 — ABP의 인천 중구+동구
  데이터를 합산해 `CCG_NM='제물포구'` 합성 행(1,918행)을 만들어 붙임.
- **추가로 같이 반영(팀원 브랜치 동기화)**: BC카드 월별 변화량
  (`bc_amt_trend_slope`, `bc_amt_cv`, `bc_amt_growth_ratio`), 성별/연령 마스킹을
  각자 컬럼 기준으로 독립 필터링, 조인 `validate='m:1'` + 행수 불변 assert.
- **결과**: 매칭률은 99.93%로 동일(원래도 "매칭"은 됐던 문제라서) — `final_joined.csv`
  29컬럼으로 갱신, 제물포구 사업장이 이제 올바른 소비 데이터를 받음.

---

## ⑧ LOCALDATA 39개 컬럼 전수 EDA + 물리적 이상치 정제

### 새로 찾은 버그: 대규모점포 1,359행이 처음부터 BC카드 매칭 불가 상태였음
- **증상**: `preprocess_localdata.py` 3단계(통합)에서 대규모점포 컬럼을 고를 때
  `large_cols_present`를 `base_cols`(원본 10개 컬럼) 기준으로만 필터링해서, 나중에
  추가한 `SIDO_NM`/`CCG_NM`이 빠졌음 → concat 시 대형할인점/슈퍼마켓 1,359행 전부
  `SIDO_NM`/`CCG_NM`이 NaN이 되어 BC카드 조인이 항상 실패. `join_datasets.py`의
  미매칭 리포트가 `groupby`에서 기본적으로 NaN 키를 숨겨서 지금까지 안 보였음.
- **수정**: `large_cols_present`를 `merge_cols`(SIDO_NM/CCG_NM/bc_업종 포함) 기준으로
  필터링하도록 변경.
- **결과**: 조인 매칭률 99.93% → **99.99%**(미매칭 1,583행 → 308행). 남은 308행은
  대형할인점의 일부 시군구×월 조합이 ABP에 원래 없거나("마산시"처럼 2010년 창원시
  통합 이전 옛 지명이 남은 주소 등), 통계적으로 무시 가능한 수준이라 추가 조치 안 함.

### 39개 컬럼 전수 EDA로 찾은 변수 후보 (`식품_일반음식점.csv` 기준 검증, 나머지 3개 파일도 동일 로직 적용)
| 컬럼 | 판정 | 근거 |
|---|---|---|
| `다중이용업소여부` | **채택** | 커버리지 100%. Y(8.7만건) 폐업률 44.9% vs N(221만건) 71.6% — 26.7%p 차이, 강한 연관 신호 |
| `시설총규모` | **채택(정제 후)** | 커버리지 100%, `소재지면적`(95%, 999.99 상한 캡 걸린 열등 필드)의 상위호환 |
| `위생업태명` | 제외 | `업태구분명`과 99.99% 동일 — 사실상 중복 |
| `여성종사자수`/`남성종사자수` | 제외 | 2010~2011년 기점으로 신고율이 65~87%→4% 미만으로 붕괴(제도/시스템 변화로 추정) — 개별 사업장 특성이 아니라 등록연도의 재인코딩에 가까움. 영업연수(이미 모델에 있는 핵심 변수)와 정보가 겹쳐 판단력을 흐릴 위험 |
| `월세액`/`보증액`/`본사직원수`/`공장*직원수` | 제외 | 값이 있는 3만여 건 중 거의 전부가 0 — 사실상 죽은 컬럼 |
| `등급구분명`/`영업장주변구분명`/`급수시설구분명` | 보류(저우선순위) | 결측 60~89%로 큼, 급수시설은 있는 값도 "상수도전용" 하나로 쏠려 변별력 낮음. 영업장주변구분명은 팀원의 중심점거리 입지 피처와 역할 겹침 |

### 물리적 이상치 정제 (`preprocess_localdata.py` 3b단계, 통합 후 일괄 적용)
> **정정(오류 수정 이력)**: 이 항목의 초안은 "EPSG:5174에서 음수 좌표는 물리적으로 불가능 → 전부 결측"
> 이라고 적고 24,743행을 결측 처리했으나 **틀린 가정이었다.** 5174는 원점이 북위 38°라서 남쪽 끝 제주는
> Y가 0 근처~음수(서귀포시 Y는 전부 음수, -42,162~-1,610), 서쪽 끝 섬(옹진군 백령·대청)은 X가 음수가
> 되는 게 정상이다. 4개 원본 파일 전체에서 음수 Y 34,159행은 100% 제주특별자치도, 음수 X 325행은 100%
> 옹진군이고 그 외 지역의 음수 좌표는 0건이다. 멀쩡한 좌표를 지운 셈이라 규칙을 아래처럼 교체하고
> `localdata_clean.csv`를 재생성했다. (`final_joined.csv`는 재생성 전 상태라 최종 조인 시 다시 만든다.)

> **2차 정정**: 위 교체안에 넣었던 "MAD 초과 + 반경 1km 이웃 3개 미만 → 결측" 규칙(고립점 2,773행)도
> 오탐 위주였다. 이 2,773행 중 82.7%는 최근접 이웃 5개가 전부 자기 주소와 같은 시군구였고 68.2%는 1km
> 안에 최근접 이웃이 있었으며 상위 지역은 홍천·상주·충주·영월·보은 등 넓은 농촌 군이었다(외딴 곳일 뿐
> 오류가 아님). 최근접이웃거리>5km 방식도 비교했으나 15행만 잡고 그중 다수가 실제 섬 점포(신안 우이도,
> 옹진 문갑도, 태안 가의도)였으며, 다른 도시 한복판에 잘못 찍힌 좌표는 원리상 못 잡는다. 그래서 결측 처리
> 규칙을 제거하고 아래 "주소-좌표 일치 검사" 플래그로 교체했다.

**좌표(`좌표정보(X)`, `좌표정보(Y)`) — 현재 규칙**
1. 한국 영토 범위 밖(X∉[-20,000, 700,000] 또는 Y∉[-60,000, 660,000]) → 결측: **0행**
   (음수 자체는 오류가 아님 — 위 정정 참고)
2. 시군구별 median 기준 4×MAD 초과: 54,633행 — **이것만으로는 오류 판정하지 않음**(넓은 시군구의 외딴
   점포일 뿐일 수 있음). 3번의 보조 조건으로만 사용.
3. **주소-좌표 일치 검사(`scipy.spatial.cKDTree`)**: 좌표 기준 최근접 5개 이웃이 전부 내 주소 시군구와
   다르면 `좌표주소불일치=1`(392행), 여기에 MAD 초과까지 겹치면 `좌표의심=1`(77행). **결측 처리 안 함, 플래그만.**
   - 불일치 392행 중 좌표의심이 아닌 315행을 분류: 258행(82%)은 주소 라벨이 ABP에 존재(시군구 경계 지역
     등으로 추정, 라벨은 정상), 57행(18%)은 라벨이 ABP에 없음(파싱 문제: 공백 없는 시+구 표기 `성남시분당구`·
     `고양시일산동구`, 구 누락 `부천시`·`창원시`, 옛 지명 `마산시`·인천 `남구`).
   - 4개 파일 통합 후 `관리번호` 중복 0행(같은 사업장이 두 파일에 중복 등록된 경우 없음).

**시설총규모**
- 3,000㎡ 초과(식당류가 현실적으로 넘기 어려운 규모, 로그변환이 아닌 결측 처리로
  틀린 값 자체를 인정하지 않는 방식 선택): **164행**
- 0값(영업 중인 매장의 물리적 크기가 0일 수 없음, 미신고로 판단): **125,706행**
- 결측 처리하되 정보를 버리지 않도록 `시설총규모_결측여부` 플래그 컬럼 추가

---

## ⑨ 컬럼 선택 방식 변경: "골라서 정제" → "규칙으로 제외한 것만 빼고 전부 정제한 뒤 EDA로 선택"

- **문제**: 기존 `base_cols`(12개)는 사람이 임의로 고른 목록이라, 나머지 컬럼은 정제도 EDA도 없이 버려졌다.
  (다중이용업소여부처럼 강한 변수를 놓쳤던 것도 이 때문.)
- **변경**: `preprocess_localdata.py`가 원본 컬럼을 전부 로드하고, 아래 **규칙 기반 제외 목록(`DROP_COLS`)** 만 뺀다.
  | 제외 기준 | 컬럼 |
  |---|---|
  | 죽은 컬럼(커버리지 ≤약 1.5% + 상수/전부 0) | 월세액, 보증액, 본사직원수, 공장사무직·판매직·생산직직원수, 전통업소지정번호, 전통업소주된음식, 건물소유구분명, 홈페이지 |
  | 다른 컬럼과 정보 중복 | 위생업태명(=업태구분명 99.99%), 영업상태코드, 상세영업상태코드 |
  | 레거시 코드 | 소재지우편번호(구 XXX-XXX 형식, 도로명우편번호로 대체) |
  | 레코드 관리 메타(폐업 시 갱신되어 누수 위험) | 데이터갱신구분, 데이터갱신시점 |
- **남은 컬럼 전부 타입별 정제(3c단계)**: 소재지면적(0→결측, 999.99 절단 의심 플래그 `소재지면적_절단의심`),
  다중이용업소여부(Y/N→`is_multiuse` 1/0), 등급구분명·영업장주변구분명·급수시설구분명·점포구분명(공백 정리 +
  결측을 실제 미기재='미기재' / 원본에 컬럼 자체가 없음='해당없음'으로 구분), 도로명우편번호(앞자리 0 복원, 5자리),
  전화번호(개인정보성이라 삭제, `전화번호_기재` 여부만 파생), 날짜 컬럼 전부 파싱.
  종사자수는 2010년 전후 신고율 붕괴가 제도 변화 때문이라 그대로 두고 EDA에서 판단.
- **산출물 2개로 분리**:
  - `data/localdata_wide.csv` — 정제된 전체 컬럼(37컬럼). **EDA 작업대.** 여기서 업종별로 후보 컬럼을 검토.
  - `data/localdata_clean.csv` — `SELECTED_COLS`(20컬럼)만. 지금은 EDA 이전의 기본값이며 **EDA 후 이 목록만 갱신**하면 된다.
- **누수 주의**: 폐업일자, 상세영업상태명, 최종수정시점은 결과(폐업)와 붙어 있어 EDA에서 연관이 커 보여도 후보에서
  제외해야 한다. 종사자수처럼 통계적 연관이 착시인 경우도 있어 채택 시 "값이 왜 이렇게 생겼는지"까지 확인한다.
- **주소 파서 수정**: 공백 없이 붙은 시+구(`성남시분당구`, `고양시일산동구`, `부천시소사구괴안동` 등)를 분리하고
  인천 `남구`(2018년 미추홀구로 개칭)를 미추홀구로 매핑. ABP 시군구에 없는 라벨 행 238 → **185행**.
  남은 185행은 구 정보가 없어 원리상 못 고치는 것들(화성시 102, 연기군 40, 부천시 10, 마산시 8, 창원시 5, 파싱실패 15).
  대형할인점/슈퍼마켓의 라벨 미존재는 29/1,359행. 이 수정으로 좌표주소불일치도 392→351행, 좌표의심 77→75행.
- 통합 시 컬럼을 이름으로 골라 붙이지 않고 4개 파일 컬럼 합집합을 그대로 쌓도록 바꿔서, 예전 대규모점포
  SIDO_NM/CCG_NM 누락 같은 종류의 버그가 구조적으로 재발하지 않는다.
- **구조적 결측 처리**: 대규모점포에만 있는 컬럼(인허가취소일자·재개업일자·휴업시작/종료일자·점포구분명)은 다른 3개 파일 행에서
  구조적으로 비고, 반대로 식품 전용 컬럼(시설총규모·is_multiuse·종사자수·등급구분명 등)은 대규모점포 행(1,358행)에서 100% 비어 있다.
  "컬럼이 원본에 없어서 비는 것"은 실제 결측과 다르므로 범주형은 `해당없음`으로 구분했고, 숫자형은 NaN으로 두되 `원본파일`로 구분 가능.
  모델링 시 이 컬럼들로 complete-case 삭제하거나 대규모점포 행을 식품 중앙값 등으로 채우면 안 된다.
  대규모점포 전용 날짜 4개는 값이 있는 행이 8~43개뿐이라 "99.9% 초과 결측 컬럼 제외" 규칙으로 통합본에서 자동 제외(`점포구분명`은 유지).

### 팀원 `step5-group-time-split` 브랜치의 전처리 변경 대조 결과 (브랜치는 아직 병합하지 않고 항목별로 이식)
| 팀원 변경 | 우리 쪽 상태 |
|---|---|
| 인천 크로스워크(제물포구=중구+동구 통합, 검단·서해←서구, 영종←중구) 공식 출처 교차확인 | 이미 동일하게 반영. 지금까지 "교차확인 필요"였던 항목이 팀원 쪽에서 확인 완료됨 |
| 대규모점포 SIDO_NM/CCG_NM 누락 버그 수정 | 이미 반영(⑧) |
| **폐업(event=1)인데 폐업일자 결측 행 제외** | **누락이었음 → 이식.** 310행(일반음식점 301, 휴게음식점 8, 대규모점포 1)을 제외. 그대로 두면 `in_bc_window`에서 폐업 사업장이 생존자로 잡힘 |
| `is_franchise`, `dist_to_region_centroid_m` 파생 | **누락이었음 → 이식**(아래 프랜차이즈 목록 수정 참고). 중심점 거리는 `좌표의심` 행 제외하고 계산 |
| `SIDO_ALIAS`(강원도·전라북도 구표기 → 신표기) | 이식(현재 데이터엔 영향 없음, 방어용) |
| 업태구분명 공백 정규화 후 매핑 | 확인 결과 현재 영향 0행(공백 정규화 전후 매핑 결과 동일)이라 이식하지 않음 |

### 프랜차이즈 키워드 목록 문제 발견 및 수정 (팀원 목록을 그대로 쓰면 안 됨)
- **재현율 문제**: `파리바게뜨`가 목록에 없고(`파리바게뜯`은 오타, `파리바게트`는 다른 표기) 3,987행 중 3,986행 누락.
  한글 표기 누락도 있음: `씨유` 16,153행 중 15,113행, `지에스25` 6,917행 중 6,420행, `지에스(GS)25` 2,496행 전부,
  `메가커피` 170행 전부 누락. 그 결과 편의점의 프랜차이즈 비율이 **52.4%**로 나왔는데 실제 편의점은 대부분 체인이라 과소 판정.
- **오탐 문제**: `CU`가 영문 단어 일부로 걸림(`cup&cake`, `PASCUCCI`, `Yacu` 등 496행).
- **수정**: 누락 브랜드/한글 표기 추가, 오타 제거, `CU`·`GS25`·`KFC`·`BBQ`는 앞뒤가 영문자가 아닐 때만 인정.
- **2차 보강**: 1차 수정 후 남은 편의점 미탐지를 사업장명 상위로 확인해 추가 — 옛 브랜드명(훼미리마트·바이더웨이·LG25·위드미),
  운영법인명(코리아세븐·비지에프리테일·에이엠피엠코리아·진로베스토아·태인로손), 띄어쓰기 변형(`이마트 24`, `GS 25`).
  한글 브랜드는 공백 제거 후 매칭. 남은 미탐지는 `GS포항장량원룸점`처럼 GS만 적힌 표기 등(모호해서 제외)과 실제 비체인(공원매점 등).
- **결과**: 편의점 52.4%→**93.8%**, 제과점 7.3%→11.8%, 전체 3.05%→4.47%. 편의점 외 업종에서 is_franchise=1 무작위 12개를
  확인했을 때 오탐은 없었음(메가커피·뚜레쥬르·이디야·씨유·커피빈·BBQ 등). 다만 정밀도는 표본 확인 수준이고 전수 검증은 아님.
- **3차 보강 + 목록을 CSV로 분리**(`franchise_brands.csv`, 전처리는 `use=1` 행만 읽음): 데이터에서 사업장명 건수로 확인한 대형 체인을 추가했다
  (메가엠지씨커피 3,677행, 본죽 1,753, 더벤티 1,482, 한솥도시락 1,472, 크라운베이커리 1,281, 엔제리너스, 공차, 요거프레소, 피자스쿨,
  이삭토스트, 엽기떡볶이, 죽이야기, 신전떡볶이, 고려당, 신라명과, 도미노·미스터피자 등). 일반어와 섞일 수 있는 짧은 이름은 브랜드 전체 표기로
  좁혔다(놀부→놀부보쌈/부대찌개/항아리, 한솥→한솥도시락, 매머드→매머드커피 등). 짧은 키워드 8개(본죽·공차·아딸·설빙·쥬씨·고려당·텐퍼센트·드롭탑)를
  매칭된 사업장명 6개씩 확인했고 모두 실제 브랜드였다.
  - **팀 결정으로 제외**: `김밥천국`(이름만 공유하는 개인 사업자 다수). **보류(use=0, 사유 기록)**: 김밥나라, 명동칼국수, 빵굽는마을, 케익하우스,
    멕시칸치킨, 신세계푸드 — 체인 여부가 모호.
  - **결과**: 전체 4.47%→6.01%, 한식계열 0.2→0.8%, 스넥 0.6→2.4%, 서양음식 4.7→8.6%, 제과점 11.8→15.7%, 편의점 93.8%.
    한식·스넥·중식의 체인 측정은 여전히 약하다(목록이 카페·제과·편의점 위주).
  - **주의**: 대형할인점(0%)·슈퍼마켓(99%)의 `is_franchise`는 정의상 의미가 없다. 슈퍼마켓은 SSM 체인 이름으로 골라 들였으므로 전부 체인이고,
    대형할인점은 이마트·롯데마트·코스트코가 있어도 목록에 없다. 이 두 업종은 생존모델에서 제외하기로 했으니 문제는 없지만 9개 업종을 합쳐 쓸 때
    이 변수를 사용하면 안 된다.
  - **API 전환은 "시간 남으면 하는 마지막 옵션"**으로 보류(공정위 브랜드 목록 등). 신호가 이미 강해서 정밀한 분류보다 전체 파이프라인 완성이 우선.
  - 프랜차이즈 관련 기존 팀원 결과(HR 0.69 등)는 재산출 대상.
- **4차 보강(한식 프랜차이즈 수동 보강)**: 한식계열 측정이 약하다는 문제에 대해 API 대신 수동 보강으로 대응. 후보를 사업장명 건수와 샘플로 확인한 뒤 32+10개 추가
  (두찜 752, 국수나무 654, 교동짬뽕 588, 등촌샤브칼국수, 큰맘할매순대국, 채선당, 가장맛있는족발, 응급실국물떡볶이, 현대옥, 역전할머니맥주, 역전우동0410,
  한촌설렁탕, 한신포차, 육쌈냉면, 빕스, 애슐리, 아웃백, 커피에반하다, 하삼동, 토프레소, 피자마루, 반올림피자 등).
  - **제외(use=0, 사유 기록)**: `본가`(해성본가·본가곱창 등 독립 상호가 대부분, 4,180행), `계절밥상`(사계절밥상 오탐), `고피자`(시카고피자 오탐),
    `스시로`(가마보꼬스시로바 오탐), `마포갈매기`(단독은 일반 식당 혼입 → `신마포갈매기`로 좁힘), `전주비빔`(메뉴명), `샹하이`, `자메이카`·`전주중앙회관`(행 11개·6개로 효과 없음).
    `연안식당`은 독립 상호가 섞였을 수 있어 정밀도 불확실한 채로 포함.
  - **결과**: 전체 6.01%→6.48%, 한식계열 0.8→1.2%, 중국음식 0.8→1.5%, 스넥 2.4→2.9%, 서양음식 8.6→9.4%. 목록은 한글 145개·영문 4개.
  - **여기서 멈춘다**: 남은 한식계열 미판정 상위 이름은 전주식당·우리식당·고향식당·진미식당 같은 일반 상호, 춘천닭갈비·남원추어탕·양평해장국 같은 지역 음식명,
    김밥천국(제외 결정), 명동칼국수(보류)뿐이라 놓친 대형 체인은 더 보이지 않는다. 한식 체인 비율 1.2%는 사업장명 기반 탐지로 도달할 수 있는 수준에 가깝고,
    이후는 수확 체감이다. 이 한계는 리포트에 명시한다(한식의 프랜차이즈 효과는 낮은 재현율로 측정됨).
- **주의**: 팀원 5단계 결과(프랜차이즈 HR 0.69 등)는 수정 전 목록 기준이라 이 변수를 쓰는 결과는 재산출이 필요함.
  팀원에게 공유할 것.
- 중심점 거리 caveat: 옹진군처럼 섬이 흩어진 시군구는 중심점이 바다 한가운데라 지표 의미가 약함.

---

## ⑩ wide 통합본 EDA — 후보 컬럼 검토 (나이·업종 보정 O/E)

- **방법**: 팀원과 같은 정의(3/31, 6/30 시점 영업 중 사업장의 이후 78일 폐업)로 위험집단을 만들고(7개 업종, 1,269,281행,
  폐업 21,544건), 업종×영업연수구간×시점별 기대 폐업 대비 관측 폐업 비 **O/E**로 후보 컬럼을 봤다. 1보다 크면 같은 나이·업종 대비
  잘 폐업한다. 원시 폐업률 비교는 나이(오래된 사업장일수록 이미 폐업했을 확률이 높음)에 오염되므로 쓰지 않았다.
  단, 이건 나이·업종만 보정한 단변량 확인이라 최종 채택은 모델의 블록 추가 검증으로 결정한다.
- **교훈**: 다중이용업소여부의 원시 폐업률 격차(44.9% vs 71.6%, 26.7%p)는 대부분 나이·시점 착시였다. 보정 후 O/E는 **0.82 vs 1.01**
  (약 18% 낮음)으로 줄었지만 업종별로 방향이 일관(0.65~0.91)하므로 실재하는 효과로 본다.

| 후보 | 보정 O/E | 판정 |
|---|---|---|
| `is_franchise` | 1 = 0.73 / 0 = 1.02. 업종별로 서양 0.44, 제과 0.36, 스넥 0.54로 일관 | **채택**(강함). 단 한식·중식 등은 브랜드 목록 누락으로 측정이 약함 |
| `is_multiuse` | 0.82 / 1.01, 편의점 제외 전 업종에서 0.65~0.91 | **채택**(중간) |
| 시설총규모 | 5분위 1.28 → 1.11 → 0.93 → 0.84 → 0.75로 단조 감소, 결측 2.71 | **채택** + `시설총규모_결측여부` |
| 소재지면적 | 시설총규모와 Spearman 1.00 | **제외**(중복) |
| `dist_to_region_centroid_m` | 1.08 → 0.83 (멀수록 낮음, 약함) | 유지 |
| 전화번호_기재 | 기재 0.91 / 미기재 1.05 (약함) | 후보로 추가 |
| 좌표결측 | 0.77 / 1.01 | 후보로 추가(결측 행을 통째로 지우면 이 집단이 사라짐) |
| 등급구분명 | 자율 1.18, 을 1.54(n=4천), 미기재 0.99. 90%가 미기재 | 약한 후보로 추가 |
| 급수시설구분명 | 간이상수도 0.58, 지하수전용 0.75(비도시 대리변수 가능성) | 약한 후보로 추가 |
| 영업장주변구분명 | 전 구간 0.92~1.04, 신호 없음 | 제외 |
| 종사자수 | 뚜렷한 단조성 없음, 77% 결측 | 제외 확정(제도 변화 착시) |
| 원본파일 | 0.97~1.01 | 신호 없음(추적용으로만 유지) |

- **시설총규모 업종 내 표준화**: 업종마다 전형적 크기가 크게 달라(편의점 중앙값 3.3㎡, 한식 60㎡) 원값은 업종 효과와 섞인다.
  `log시설총규모_업종내z`(log 후 업종별 z-score)를 만들었다. **편의점은 값의 31.5%가 3.3㎡ 하나로 몰려 있어**(다른 업종의 최빈값
  비중은 0.4~1.1%) 플레이스홀더로 보고 0(업종 평균)으로 처리했다. 이로써 "편의점 시설총규모 3.3㎡ 처리" 열린 이슈는 종결.
- **`localdata_clean.csv`**: 22 → **27컬럼**(전화번호_기재, 등급구분명, 급수시설구분명, 좌표결측, log시설총규모_업종내z 추가).
  wide는 37컬럼.
- 한계: O/E는 지역·다른 변수를 보정하지 않았고, 일부 수준은 폐업 건수가 적다(편의점 is_multiuse=1은 n=105, 을 등급은 59건).
- **재실행 가능한 점검 스크립트 `eda_candidates.py`** (팀원 브랜치의 `eda.py`와 이름 충돌 방지): 위 스크리닝을 그대로 재현하고
  `output/eda_candidates_oe.csv`로 저장. 전처리·프랜차이즈 목록·외부 데이터를 바꾼 뒤 다시 돌려 후보 판정을 갱신하는 용도.
  판정은 이 스크립트가 아니라 모델 블록 검증으로 하며, 신호 없다고 wide에서 컬럼을 지우지 않는다(clean은 후보 풀).
- **LOCALDATA만으로 만든 그룹(시군구×업종) 파생 후보 점검** — landmark 이전 정보만 사용(누수 없음):
  | 파생 후보 | 5분위 O/E | 판정 |
  |---|---|---|
  | `g_closure_rate_1y`(직전 1년 폐업률) | 0.76 → 0.93 → 1.01 → 1.10 → 1.16 단조 증가 | **강한 후보** |
  | `g_opening_rate_1y`(직전 1년 개업률) | 0.86 → 1.13 | 폐업률과 Spearman 0.64, 중복 |
  | `g_log_open`(그룹 내 영업 중 점포 수) | 0.95 → 1.06 | 약함 |
  landmark 시점에 따라 값이 달라지므로 wide/clean의 정적 컬럼이 아니라 **모델링 스크립트에서 시점별로 계산**해야 한다.
  주의: 이 변수는 지역×업종의 "자기 이력 기반 기저 위험"이라, BC카드 변수의 추가 기여는 이 변수를 통제한 뒤에도 남는지로 다시
  검증해야 한다(BC카드 기여가 줄어들 수 있음). 지자체별 폐업 신고 처리 관행이 섞여 있을 가능성도 있다.

---

## ⑪ landmark 설계의 "1~3월 폐업 누락(survivorship)" 정량 확인

- **배경**: landmark(3/31, 6/30 시점 영업 중 사업장의 이후 78일)는 1~3월에 이미 폐업한 사업장을 위험집단에서 통째로 뺀다. 팀원 CHANGELOG의
  한계 목록에 없어 확인이 필요했고, 지금까지 미확인 상태였다.
- **빠지는 규모**(7개 업종, 2026-01-01~09-16 폐업 40,793건): 1~3월 13,708건(33.6%) + 6/17~6/30 공백 2,340건(5.7%) →
  landmark(L1+L2)가 다루는 사건은 24,745건 = **60.7%**. 약 39%는 분석에 안 들어간다.
- **빠지는 집단은 실제로 다르다**: 1~3월 폐업 사업장은 남는 집단(L1 위험집단)보다 영업연수 8.3년 vs 11.8년, 프랜차이즈 5.9% vs 8.7%,
  시설총규모 z −0.02 vs +0.20, 서양음식 비중 26.4% vs 21.9%. 위험이 높은 유형이 먼저 빠지는 depletion 패턴과 일치한다.
- **그러나 사업장 단위 효과의 크기는 창을 바꿔도 사실상 같다**(나이·업종 보정 O/E, W0=1/1 시작 / L1 / L2):
  프랜차이즈 0.70 / 0.70 / 0.77, 다중이용 0.82 / 0.83 / 0.81, 시설총규모 작음 1.32 / 1.34 / 1.32, 큼 0.78 / 0.79 / 0.78.
  즉 1월 1일부터 시작하는 전체 코호트로 봐도 측정된 사업장 변수의 효과는 달라지지 않는다 → 측정 변수에 대한 편향 증거는 없다.
- **해석**: 이 설계는 "3/31 또는 6/30까지 살아남은 사업장 중 누가 78일 안에 폐업하는가"라는 조건부 질문에 답하며 landmark 방식의
  표준적 정의다. 문제는 편향보다 **다루는 범위의 한계**이므로 리포트에 "2026년 폐업의 약 61%만 분석 대상" 임을 명시한다.
  BC카드(그룹 단위) 변수의 효과가 창에 따라 안정적인지는 1/1 시점에 BC 직전 기간이 없어 확인 불가.
- **BC 트렌드 변수의 성능 기여(팀원 S5 민감도, `output/5_모델성능.csv`)**: M3(추세·변동성 포함) vs S5(제외) C-index —
  train 0.6218 vs 0.6216, valid_time 0.6109 vs 0.6114, test_group 0.6306 vs 0.6302, **test_both 0.6068 vs 0.6068**.
  차이 ±0.0005 이내(부호 혼재)로 잡음 수준이고, RSF 블록 중요도도 0.00003 ± 0.002. 단 검증된 것은 "직전 3개월 월 3개 점으로 만든
  기울기·변동성"에 한정되며, 계절 효과는 두 landmark가 계절과 겹쳐 분리 불가라 검증되지 않았다. 단순 코호트로 가면 트렌드
  변수를 못 쓰지만 이 손실은 성능상 무시 가능. 시간 검증 불가·BC를 추적 기간 중 값으로 붙여야 하는 점은 별개로 남는다.
- **참고**: 월별 폐업은 2026-01~08이 3,700~5,400건으로 안정적이고 7~9월에 눈에 띄는 신고 지연 감소는 없다. 2025-12만 9,678건으로
  이례적으로 많아 연말 일괄 정리 가능성이 있다(폐업 = 실제 실패가 아닌 행정 처리가 섞일 수 있음). 제과점은 78일 폐업이 L1 570건 →
  L2 279건으로 절반 — 원인 미확인.

---

## ⑫ 확정된 설계 결정 (2026-09-20)

### 결정 1 — 시간 설계: 단순 코호트를 주 설계, landmark(L1/L2)는 강건성 확인
- **근거**: (a) 사업장 변수의 효과가 1/1·3/31·6/30 시작 모두에서 같음(프랜차이즈 0.70/0.70/0.77 등, ⑪), (b) BC 트렌드 변수는 성능 기여가
  0.0000~0.0005라 1/1 시점에 직전 BC가 없어 트렌드를 못 쓰는 손실이 무시 가능(팀원 S5), (c) 설명이 쉽고 사건이 더 많음(1/1~9/16 폐업
  약 4만 건 vs landmark가 쓰는 약 2.5만 건). 실제 유효 표본은 사업장 수가 아니라 시군구×업종 그룹 수(약 1,800)라는 한계는 두 설계에 공통.
- **주 설계 정의(기본값, 이견 시 조정)**:
  - 모집단: 7개 업종에서 2026-01-01에 영업 중인 사업장(인허가일자 ≤ 1/1, 폐업일자 없음 또는 ≥ 1/1). 2026년 신규 개업 사업장은 제외.
  - 시간 원점 1/1, 사건 = 폐업일자가 추적 기간 안. 추적 종료일은 **6/30**(BC 관측기간과 일치)을 기본으로 하고, 9/16(LOCALDATA 종료)까지
    연장한 결과를 민감도로 함. 종료일까지 폐업하지 않으면 censored.
  - 사업장 변수: log 영업연수(1/1 기준), is_franchise, 시설총규모(업종내z, 결측여부), is_multiuse, 중심점 거리 등.
  - 그룹 변수: BC 6개월 집계(성별·연령 구성비, 규모)와 `g_closure_rate_1y`(1/1 이전 1년 기준). BC는 추적 기간 중 값이라는 점을 한계로 명시.
  - 검증: 시군구×업종 그룹 단위 분할, 시군구 cluster bootstrap, 블록 joint 검정(팀원 검증 장치 재사용).
- **강건성**: 팀원 landmark(L1=3/31, L2=6/30, 78일), 추적 종료 9/16 연장.
- **주의**: 위 "W0"는 분석 중 쓴 1/1 시작 창의 별칭이었고, 확정된 주 설계는 위의 단순 코호트다.

### 결정 2 — 업종: 데이터는 9개, 생존모델은 7개
- 생존모델(Cox/RSF): 한식계열·일식회집·중국음식·서양음식·스넥·제과점·편의점.
- **대형할인점(2026 폐업 14건)·슈퍼마켓(9건)은 모델에서 제외하고 KM(전체 이력: 폐업 누적 114건·210건)과 BC 단독 분석**으로 다룬다.
  리포트에 사건 수 부족으로 제외했음을 숫자와 함께 명시. 슈퍼마켓은 LOCALDATA에 SSM 체인만 있어 BC 슈퍼마켓 소비(대부분 개인슈퍼)와 대상이
  어긋나는 한계도 함께 명시.
- `final_joined`·wide·clean은 9개 업종 전부를 유지한다. 점검 스크립트(`eda_candidates.py`)도 9개 업종 기준.

### 지역 키 기준표 (`build_region_master.py` → `data/region_master.csv`, `data/region_unmatched.csv`)
- ABP 255개 시군구 + 합성 1개(인천 제물포구) = **256개**. LOCALDATA 행의 **99.991%**(2,144,621 / 2,144,806)가 기준표에 대응.
  표준에 없는 라벨은 11종 185행(화성시 구 없음 102, 연기군 40, 파싱 실패 15, 부천시 10, 마산시 8, 창원시 5 등) → `region_unmatched.csv`.
- 특수 처리를 `비고` 열에 기록: 세종(SIDO=CCG), 전남광주통합특별시 분리, 인천 검단·서해→서구 / 영종→중구 / 제물포구 합성, 남구→미추홀구, 화성 4구.
- **인천 동구는 LOCALDATA 행이 0개**: 옛 동구 사업장이 전부 `제물포구`(5,761행)로 표기돼 있기 때문(정상).
- **한계**: ABP 중구는 옛 중구 전체(영종+내륙)라 LOCALDATA의 영종구 사업장은 내륙 소비까지 섞인 BC 값을 받는다.
- **`개방자치단체코드`는 지역 키로 못 쓴다**: 한 시군구에 코드가 2~4개(광명시 4개 등)라 1:1이 아님. 외부 데이터용 `시군구코드_외부`는
  소진공 상가정보 등 실제 원본의 코드로 채워야 하므로 비워뒀다(추정 입력 금지).

### ⑬ 조인 재생성 결과 (최신 정제본 기준)
- `final_joined.csv`: 2,144,806행 × 42컬럼. 매칭률 **99.99%**(미매칭 255행: 화성시 구 정보 없음 등 `region_unmatched.csv`와 같은 계열).
- `bc_group_month.csv`: 13,447행 = 2,244개(시군구×업종) 그룹 × 6개월(일부 그룹은 월이 빠져 이론값 13,464보다 17행 적음). 제물포구 합성 그룹 포함.
- `in_bc_window`(2026-01~06과 존속기간이 겹침) = 664,500행. **주 설계의 코호트(1/1에 영업 중인 사업장)와는 정의가 다르다**: 이 플래그는 1/1 이후 개업한
  사업장도 포함하므로 모델링에서는 `인허가일자 ≤ 2026-01-01` 이고 (`폐업일자` 결측 또는 `≥ 2026-01-01`)로 코호트를 직접 만들어 쓴다.
- 트렌드 컬럼(`bc_amt_trend_slope` 등)은 성능 기여가 없어(⑪) 주 설계에서는 쓰지 않지만 삭제하지 않고 남겨둔다.

### 다음 순서
1. ~~프랜차이즈 목록 보강(대형 체인 누락 채우기 + 목록을 코드 밖 CSV로 분리)~~ — 완료(⑨ 3차 보강, `franchise_brands.csv`)
2. ~~`join_datasets.py` 수정: `final_joined`(정적 BC 공변량)는 유지하고, landmark 강건성용 (시군구, 업종, 월) 단위 `bc_group_month` 표 추가~~ — 완료(⑬)
3. 주 설계(단순 코호트) 모델링 스크립트 작성 또는 팀원 코드의 코호트 생성부 교체 — 데이터 확정 후 팀원이 `final_joined`로 전체 재실행
   (이때 S5 트렌드 제외 비교도 함께). 프랜차이즈 관련 기존 숫자(HR 0.69 등)는 재산출 대상.
4. 소진공 경쟁밀도(사업장 단위, 관리번호로 결합)와 외부 시군구 변수는 그 이후. GNN은 기본 모델 잔차의 공간 자기상관(Moran's I)을 본 뒤 결정.

---

## ⑭ 5단계 모델링 (팀원 step5-group-time-split 브랜치, 팀원 CHANGELOG의 ⑦) (`cox_rsf.py`) — 검증 후 재설계 (v2)

### 질문 범위 (정정)
- 이 모형은 **개업 후 전체 생존시간을 설명하지 않는다.** 2026년 landmark(3/31, 6/30) 시점에 영업 중인 사업장이 **이후 78일 안에 폐업하는지의 위험 순위/확률**을 본다.
- BC카드 변수는 사업장별이 아니라 시군구×업종 그룹 단위 값이다. 결과는 "그 사업장이 속한 지역×업종의 소비 구조와 단기 폐업 위험의 연관"이지, 그 가게 고객의 특성이 폐업을 일으킨다는 뜻이 아니다(ecological).
- 4단계 KM(개업 후 수년 생존)과 5단계(2026년 단기 hazard)는 시간축이 다르다. KM의 지역 편차를 2026년 BC카드로 설명하는 근거로 쓰지 않는다.

### v1(이전 커밋)에서 정정한 문제
| 문제 | 조치 |
|---|---|
| "처음 보는 지역"이라 썼지만 분할 단위는 시군구×업종. test 시군구 191개가 **전부(191/191) train에도 존재** | 헤드라인을 "미학습 시군구×업종 그룹"으로 바꾸고, **시군구 통째 hold-out**을 별도 평가로 추가 |
| 그룹 분할 근거를 "BC카드·경쟁밀도가 그룹 단위"라고 씀. 모델의 경쟁밀도는 사업장별(500m) | 근거는 **BC카드 변수가 그룹 단위**이기 때문이라고 정정 |
| 경쟁밀도가 2026-06-30 스냅샷인데 L1(3/31 기준) 학습에 사용 = 결과 기간 뒤에 측정된 값 | **주모형에서 경쟁밀도 제외.** 누수 민감도(S1, S2)로만 포함 |
| L1 관측창 91일, L2 78일로 불일치 | 두 landmark 모두 **78일**로 통일(L1: 3/31~6/17, L2: 6/30~9/16) |
| "가게 특성만" 모형이 업종 더미를 포함 | 모형을 M0 업종만 / M1 +영업연수 / M2 +프랜차이즈·입지 / M3 +BC카드로 **단계별 분해** |
| RSF permutation이 행 단위라 그룹 구조·구성비 합·one-hot을 깨뜨림 | **블록 permutation**: 그룹 변수는 그룹 통째로 다른 그룹 값과 교환, 구성비·업종은 블록 전체를 함께 교환, 10회 반복 |
| 표준오차 bootstrap 50회, 클러스터가 시군구×업종 | **시군구 단위** cluster bootstrap 500회, 95% 신뢰구간 제시 |
| C-index +0.0015의 유의성·분할 1회 | M3−M2 차이의 **paired 시군구 bootstrap CI**(B=300) + **10회 반복 분할**(그룹/시군구 hold-out) |
| 개별 p값만으로 "BC카드 비유의" 결론 | **블록 joint 검정**(Wald, LR) 추가. 개별로는 비유의여도 블록 전체는 유의 |
| "위험도 보정"이 순위 확인일 뿐 | 예측확률 `1 - S(78일)` 기준 **calibration, Brier, AUC** 추가 |
| PH 가정 미검증 | Schoenfeld 검정 추가 |
| 그룹 공변량 조인의 행 증식 방어 없음 | `validate="m:1"` + 행 수 불변 assert (`cox_rsf.py`, `join_datasets.py`) |
| empirical Bayes를 test 그룹까지 포함해 추정, 금액 가중 구성비에 이항분산 근사 | 주모형에서 제외. 민감도 S3로만 유지하되 **prior를 train 그룹에서만 추정** (엄밀한 posterior가 아닌 근사) |
| 성별x/연령x 마스킹 | 두 마스킹은 **항상 같은 행**(13,161행 모두)에서 발생. 코드 주석의 "독립 마스킹"은 잘못이라 정정. x 금액비중을 민감도(S4)로 추가 |

### 분할과 표본
- 모집단: landmark 시점에 영업 중인 사업장. L1 634,761행(폐업 11,021건, 1.74%), L2 634,520행(폐업 10,523건, 1.66%). 좌표 결측 등 공변량 결측 제거 후 1,227,022행.
- 그룹(시군구×업종) 1,782개, 시군구 255개. 주분할은 그룹 단위 80:20(업종 stratify), seed 42.

| 세트 | 그룹 | 시점 | 행 | 폐업 |
|---|---|---|---|---|
| train | 학습 1,424 | L1 | 501,771 | 8,637 |
| valid_time | 학습 1,425 | L2 | 501,427 | 8,553 |
| test_group | 미학습 357 | L1 | 111,925 | 1,995 |
| test_both | 미학습 357 | L2 | 111,899 | 1,757 |

- `valid_time`은 학습과 같은 그룹·상당수 같은 사업장의 이후 시점이라 **독립 표본이 아니다.** 시간 일반화 진단용이다.
- L1(BC 1~3월)과 L2(BC 4~6월)는 계절이 다르다. 시간 일반화 결과에는 **계절 효과가 섞여 있으며 분리하지 못했다.**
- 좌표 결측 사업장은 L2 모집단의 3.3%(21,164행)로 모형에서 제외된다. 이들의 78일 폐업률은 1.01%로 좌표가 있는 사업장(1.68%)보다 낮고, 업종 구성도 다르다(한식 49% vs 54%). 결과는 좌표가 있는 사업장에 대한 것이다.

### 결과 (Cox, 주분할. C-index, 0.5=무작위)
| 모형 | train | valid_time | test_group | **test_both** | test_both 업종 내 |
|---|---|---|---|---|---|
| M0 업종만 | 0.541 | 0.518 | 0.531 | 0.521 | 0.500 |
| M1 +영업연수 | 0.614 | 0.599 | 0.619 | 0.600 | 0.599 |
| M2 +프랜차이즈·입지 | 0.618 | 0.605 | 0.623 | 0.604 | 0.604 |
| M3 +BC카드 | 0.622 | 0.611 | 0.631 | **0.607** | 0.607 |
| RSF (M3 변수) | 0.645 | 0.614 | 0.627 | 0.605 | 0.601 |

민감도(test_both): S1 M2+경쟁밀도(누수) 0.607, S2 M3+경쟁밀도(누수) 0.608, S3 M3 구성비 EB 0.607, S4 M3+마스킹(x) 금액비중 0.607, S5 추세·CV 제외 0.607. 경쟁밀도·EB·추세·CV는 결과를 바꾸지 않았다.

**M3−M2 (BC카드의 추가 기여)**
- 주분할 test_both: **+0.0026, 95% CI [−0.0008, +0.0058]** (시군구 cluster bootstrap). 0을 포함.
- 10회 반복 분할 평균: 그룹 분할 **+0.0058 (sd 0.0036)**, 시군구 hold-out **+0.0042 (sd 0.0050)**.
- 분할에 따라 C-index가 ±0.01 흔들린다. 모형 간 차이(0.005 안팎)가 분할 변동보다 작아 순위를 확정적으로 말하기 어렵다.

**반복 분할 (test_both C-index 평균, 10회)**
| 모형 | 그룹 분할 | 시군구 hold-out |
|---|---|---|
| M0 업종만 | 0.517 | 0.512 |
| M1 +영업연수 | 0.598 | 0.601 |
| M2 +프랜차이즈·입지 | 0.604 | 0.606 |
| M3 +BC카드 | 0.609 | 0.610 |

시군구 통째 hold-out에서도 결과가 거의 같다. 그룹 분할의 결과가 시군구 겹침 때문에 부풀려졌다는 근거는 이 데이터에서는 없다.

**Cox M3 계수 (시군구 cluster bootstrap B=500, 95% 신뢰구간)**
- 영업연수 log(1+연수): HR 0.69 [0.67, 0.72] (변수가 1 커질 때. 연수 1년이 아님)
- 프랜차이즈: HR 0.69 [0.61, 0.77]. 다른 변수로 조건화한 **관찰된 연관**이며 인과효과가 아니다.
- 중심점까지 거리(log): HR 0.94 [0.91, 0.97]
- BC카드 9개 변수: **개별로는 모두 신뢰구간이 1(계수 0)을 포함**한다. 그러나 **블록 joint 검정은 유의**하다(Wald χ²=50.8, df=9, p=7.7e-8. LR p=3.6e-17이나 사업장 독립을 가정해 반보수적). 구성비 변수들끼리 정보가 겹쳐 개별 p로는 블록 효과를 판단할 수 없다.
- 성별·연령 비중은 합이 1인 구성비이며 준거범주(성별 3=법인, 연령 1)를 뺀 대비다. 개별 계수를 독립 효과로 해석하지 않는다. 구성비 10%p 변화당 HR은 `5_CoxPH_계수.csv`의 `HR_per_10pp`.
- PH 가정(Schoenfeld, 10만 행 표본): 위반 신호 없음(최소 p=0.069, 프랜차이즈). 업종은 더미이므로 공통 baseline에 곱해지는 상대위험이며 업종별 baseline 형태는 허용하지 않는다.

**78일 고정기간 지표 (test_both)**
| 모형 | AUC | Brier skill (학습 폐업률 상수 예측 대비) |
|---|---|---|
| Cox M2 | 0.605 | 0.0017 |
| Cox M3 | 0.608 | 0.0018 |
| RSF | 0.606 | 0.0026 |

- 확률 예측의 개선폭은 매우 작다. 폐업이 드문 사건(1.6%)이고 신호가 약해서, 순위는 어느 정도 구분하지만 개별 사업장의 확률을 정밀하게 맞히지는 못한다.
- Calibration(예측확률 십분위): Cox M3는 1~9분위에서 예측과 실제가 가깝고(예 1분위 0.79% vs 0.79%), **최상위 10분위는 과대예측**(예측 3.29% vs 실제 2.27%). RSF는 최상위에서도 더 가깝다(2.90% vs 2.65%).

**RSF 블록 permutation 중요도 (test_both 표본 40,000행, 10회 반복, 평균 ± sd, C-index 하락폭)**
| 블록 | 중요도 |
|---|---|
| 영업연수 | 0.083 ± 0.006 |
| BC 연령 비중(5개) | 0.013 ± 0.003 |
| BC 성별 비중(2개) | 0.004 ± 0.005 |
| 중심지까지 거리 | 0.003 ± 0.003 |
| 프랜차이즈 | 0.001 ± 0.000 |
| 업종 더미 | 0.001 ± 0.001 |
| BC 추세·변동성(2개) | 0.000 ± 0.002 |

v1의 "BC카드 변수는 모두 0.003 이하"는 구조를 깨뜨린 permutation의 결과였다. 블록으로 보면 **BC 연령 구성비가 영업연수 다음으로 크고 0보다 분명히 크다**(10회 모두 0.0099 이상). 다만 영업연수의 약 1/6 수준이다. 음수·0 근처 값은 "기여 0"이라기보다 표본 변동과 상관 구조의 영향일 수 있어 잡음이라고 단정하지 않는다.

### 해석 (이 결과가 지지하는 범위)
- 예측력의 대부분은 **영업연수 하나**에서 나온다(M1이 M3 성능의 대부분).
- **2026년 상반기 BC카드 9개 집계 변수는 78일 폐업 순위 예측에 작지만 일관된 추가 정보**를 준다(블록 joint 유의, 반복 분할 평균 +0.004~0.006, RSF에서 연령 구성비가 2위). 다만 주분할 단일 비교의 차이는 신뢰구간이 0을 포함하고 절대 크기는 작다.
- "BC카드 소비 변수는 유의하지 않다"는 v1의 결론은 개별 p값만 본 잘못된 결론이었다.
- 이 결론은 "2026년 6개월 BC 데이터의 특정 9개 집계 특징 → 약 78일 폐업 순위 예측"에 한정된다. BC카드 소비 전반에 대한 결론이 아니다.

### 한계
- 관측창 78일, BC카드 6개월뿐이라 landmark 2개가 최대. 계절 효과 분리 불가.
- 월 자료 3개로 만든 추세 기울기·변동성은 불안정하다("최근 3개월 단순 변화" 수준). S5에서 제외해도 결과가 같다.
- RSF는 한 가지 설정(10만 행, 100그루, depth 8)만 시험했고 튜닝하지 않았다. "모델을 복잡하게 해도 안 오른다"고 단정하지 않는다.
- Cox의 train-test 격차가 작은 것을 "과적합 없음"의 증거로 쓰지 않는다(신호가 약해서 train도 못 맞춘 것일 수 있음).
- C-index는 사업장 수가 큰 그룹에 좌우된다. 그룹별 macro C-index는 그룹당 폐업이 평균 약 5건이라 계산할 수 없어 **업종 내 C-index**(위 표)로 대신했다.
- 소진공 경쟁밀도는 자기 점포가 포함됐을 수 있고(매칭 키 없음), 일부 업종 매핑(동남아시아·비알코올→서양음식, 아이스크림·빙수→제과점)은 BC 분류 체계에 맞춘 근사다. 주모형에서 제외했으므로 주결과에는 영향이 없다.
- 프랜차이즈 HR, 영업연수 HR은 인과가 아닌 연관이며 선택효과·입지 등 미측정 교란이 남는다.
- 미적용: 지역별 폐업률에 대한 부분 풀링, 직권취소 128건의 event 재분류(분석 대상 업종에는 해당 행 없음).

### 4단계(KM) 보완과 주의
- 지역×업종 KM은 사업장 30개 이상 **그리고 폐업 30건 이상**인 그룹만(1,616개) 남기고 5년 생존율의 95% CI(Greenwood)를 함께 낸다(`4c_지역x업종_KM.csv`). 극단 지역은 대부분 그대로 남았고 CI도 대체로 좁다.
- **개업 코호트별 5년 생존율이 크게 다르다**(`4d_개업코호트별_5년생존율.csv`): 1999년 이전 개업은 한식 80%, 중국음식 88%인데 2005~2021년 개업은 업종에 따라 44~68%(한식 56~61%, 중국음식 63~68%, 제과점 44~53%). 편의점은 예외로 오래된 코호트가 오히려 낮다(61%). 시대 차이일 수도 있지만 오래된 코호트에서 초기에 폐업한 사업장의 기록이 덜 남았을 가능성도 있어, **전체 이력을 합친 KM은 장기 생존율을 과대추정했을 수 있다.** 원인은 확인하지 못했다.

### 반영하지 않은 지적과 이유
- **성별 구성비 2개 완전 공선성**: 성별 범주는 1, 2, 3(법인)이라 두 변수의 합이 1이 아니고 3이 준거범주다.
- **`rng` 미정의**: 실제 `cox_rsf.py`에는 정의돼 있고 실행도 끝까지 됐다(문서 발췌에서 그 줄이 빠졌을 뿐).
- **폐업 event 정의 불일치 / 취소·말소류 censor**: 분석 대상 7개 업종의 영업상태는 `영업/정상`과 `폐업` 둘뿐이고 `event_observed==1`과 `폐업일자 존재`가 완전히 일치한다. 취소·말소류는 대규모점포(분석 제외)에만 있다. 결과에 영향이 없어 코드를 바꾸지 않았다.
- **구성비 준거범주 미제외**: 이미 성별 3, 연령 1을 뺐다.

### 재현 정보
- Python 3.9.6, 패키지 버전은 `requirements.txt`(pandas 2.3.3, numpy 2.0.2, lifelines 0.30.0, scikit-survival 0.23.1, scikit-learn 1.5.2, scipy 1.13.1).
- seed: 주분할·RSF 42, 반복 분할 1000~1009, Cox cluster bootstrap 2000+i, paired bootstrap 3000+i.
- 입력 파일 sha256(앞 16자리): `final_joined.csv` c7c2f4d03ad7b979, `bc_clean.csv` d5040a582dde1b73, `5b_spatial_competitor.csv` 6315b9f2d8b489df.
- 실행: `python3 -u cox_rsf.py` (전체, 약 2시간), `python3 cox_rsf.py --biz 편의점 --n-boot 20 --n-diff-boot 20 --n-repeat 2 --n-perm 2 --rsf-n 20000 --rsf-trees 20` (안전판).

---

## ⑮ `cox_rsf.py`에 단순 코호트 설계 반영 (팀원 브랜치 병합 후 로컬 수정)

- **바꾼 것(코호트 생성부 중심)**: `--design cohort|landmark`(기본 **cohort**)를 추가했다. cohort는 2026-01-01에 영업 중인 사업장 전체를 `--end`(기본 6/30,
  민감도 9/16)까지 추적한다(관측창 180일). 팀원의 정의(`인허가일자 ≤ 시작일`, `폐업일자` 결측 또는 `> 시작일`, 사건이면 폐업까지 일수, 아니면 관측창)를 그대로
  재사용하고 `landmark` 원안은 `--design landmark`로 그대로 남겼다. **산출 파일 이름에 설계명이 붙어**(`..._cohort`, `..._landmark`) 팀원의 기존 결과(`output/5_*.csv`)를
  덮어쓰지 않는다.
- **cohort에서 달라지는 것**: 시간 분할이 없어 세트가 `train`/`test_group` 둘뿐이고(`valid_time`·`test_both` 없음, 평가 세트는 `TEST_KEY`), BC는 룩백 없이
  2026-01~06 전체 집계이며, 트렌드·CV 변수는 성능 기여가 0(⑪)이라 뺀다. 그룹 분할, 시군구 cluster bootstrap, 블록 joint 검정, RSF 블록 permutation, calibration은
  그대로다(단 calibration 등의 horizon은 `H_YEARS`=180일).
- **모형 구성(양 설계 공통)**: M0 업종 / M1 +영업연수 / M2 +프랜차이즈·입지 / **M2x +사업장 확장변수**(is_multiuse, size_z, size_missing, coord_missing, phone_recorded) /
  **M2h +지역 최근폐업률**(`g_closure_rate_1y`, 시작 시점 이전 1년만 사용) / M3 +BC카드. **BC카드의 순수 추가 기여는 M3 − M2h**로 보고(사업장 변수와 지역 이력을 모두 통제),
  M3 − M2는 참고로 함께 낸다. LR 검정도 M2h 대비. 경쟁밀도 민감도 S1·S2는 `5b_spatial_competitor.csv`가 있을 때만 실행한다.
- **표본 정의 변경**: 좌표가 없는 사업장(약 3%)을 더 이상 제거하지 않는다. 거리·경쟁밀도는 중앙값으로 채우고 `coord_missing`을 넣는다(폐업 양상이 달라 통째 제외하면 선택편향).
  결측/무효 시설총규모는 업종 평균(0)으로 두고 `size_missing`을 함께 넣는다. 업종 하나만 돌려 상수가 되는 열(편의점의 `size_z`)은 자동으로 제외한다.
- **코호트 생성 검증(실행 결과)**: 1/1에 영업 중인 7개 업종 사업장 **635,567개**(앞서 EDA의 1/1 창과 일치), 180일 폐업 **25,330건(3.99%)**(팀원 landmark 한 창의 약 1.1만 건의 2배).
  그룹 1,782개·시군구 255개, 그룹 분할 train 524,178행(폐업 20,981) / test_group 111,358행(폐업 4,348). `g_closure_rate_1y`는 0~2.7, 모형 변수에 결측 없음.
- **스모크 테스트**: 편의점 한 업종을 작은 옵션(`--n-boot 20 --n-diff-boot 20 --n-repeat 2 --skip-rsf`)으로 cohort·landmark 두 설계 모두 끝까지 통과(landmark 편의점
  위험집단 469/514건은 EDA 값과 일치). **표본이 작고 부트스트랩이 20회뿐이라 이 실행의 C-index·차이 값은 해석 대상이 아니다.** 전체 7개 업종 본 실행은 아직 안 했다.
- **아직 못 한 것 / 환경 주의**:
  - `scikit-survival`은 이 컴퓨터의 Python 3.13용 설치 조합이 없어 **RSF는 로컬에서 실행·검증하지 못했다**(`--skip-rsf`로 Cox만 검증). RSF 블록 정의를 바꿨으므로(사업장 확장변수·
    지역 최근폐업률 블록 추가) 팀원 환경(Python 3.9, `requirements.txt`)에서 한 번 돌려 확인해야 한다. `sksurv` import는 RSF를 돌릴 때만 하도록 늦췄다.
  - 가상환경 `.venv`(시스템 패키지 공유 + `lifelines` 설치)를 프로젝트 안에 만들어 썼고 `.gitignore`에 넣었다. 전역 환경은 건드리지 않았다.
  - **윈도우 사용자 이름이 한글이면 joblib 병렬이 실패**한다(임시 폴더 경로를 ASCII로 인코딩하려다 `UnicodeEncodeError`). `JOBLIB_TEMP_FOLDER`를 ASCII 경로(예: `C:\joblib_tmp`)로 지정하면 해결된다.
  - 본 실행은 시군구 bootstrap 500회 × 여러 모형이라 오래 걸린다(팀원 기록 기준 landmark 전체 약 2시간, 코호트는 행이 적지만 사건이 많아 비슷하거나 더 걸릴 수 있음).
- **실행 예**: 안전판 `python cox_rsf.py --biz 편의점 --n-boot 20 --n-diff-boot 20 --n-repeat 2 --skip-rsf` / 본 실행 `python cox_rsf.py` / 민감도 `--end 2026-09-16` /
  강건성 `--design landmark`.

### 팀원 나머지 스크립트 점검 결과 — 설계(코호트)에 의존하는 건 `cox_rsf.py`뿐
| 스크립트 | 역할 | 수정 필요? | 비고 |
|---|---|---|---|
| `cox_rsf.py` | 5단계 Cox/RSF | **수정함**(⑮) | 코호트·시간·분할이 여기에만 있음 |
| `km_screening.py` | 4단계 업종·지역·개업코호트 KM(전체 이력) | **소폭 수정함** | 아래 참고. 설계와는 무관 |
| `eda.py` | 1단계 기술통계 EDA | 코드 수정 불필요 | 재실행하면 새 데이터 기준 수치로 갱신. 단 `CUTOFF=2026-09-28`이 다른 스크립트(09-16)와 달라 생존시간 기준일이 불일치 |
| `safety_net.py` | 0단계 편의점 KM 안전판 | **삭제함** | 파이프라인 검증용 일회성 스크립트이고 `cox_rsf.py --biz 편의점` 스모크 실행이 그 역할을 대신한다. git 이력에서 복구 가능 |
| `spatial_features.py` | 사업장별 반경 500m 경쟁밀도 → `5b_spatial_competitor.csv` | 재실행 불필요 | 입력(`localdata_clean.csv`의 관리번호·업종·좌표)이 그대로이고 5b가 7개 업종 사업장 2,143,448행을 **100%** 포함(좌표 없는 150,701행은 NaN). 소진공 원본(`data/sojin_sanga`)은 이 컴퓨터에 없음 |
| `sojin_density.py` | 소진공 상가정보 → **시군구×업종 점포 수** 표(`5_경쟁밀도_시군구x업종.csv`) | **유지** | `cox_rsf.py`는 이 표를 쓰지 않는다(사업장별 500m인 5b를 씀). 그러나 이 표에는 LOCALDATA에 없는 **개인 슈퍼마켓**(63,862개)이 들어 있어 슈퍼마켓 공급 측 보완과 그룹(시군구×업종) 단위 경쟁밀도에 쓸 수 있다 |
- **`km_screening.py` 수정 내용**: (1) 주석에는 "대형할인점·슈퍼마켓은 별도 취급"이라 되어 있으나 코드가 없어서, 확정한 결정(⑫)대로 이 두 업종의 전체 이력 KM을 추가했다
  (`4e_대형할인점_슈퍼마켓_KM.png`, `..._요약.csv`). 결과: 대형할인점 n=744·폐업 114건, 중위생존 미도달, 5년 생존 0.960, 10년 0.932 /
  슈퍼마켓 n=614·폐업 210건, 중위생존 19.3년, 5년 0.885, 10년 0.709(SSM 체인만이라는 한계 있음). (2) `usecols`로 읽는 컬럼을 6개로 제한(42컬럼 전체를 읽으면 메모리 부담).
  (3) 한글 글꼴을 `AppleGothic → Malgun Gothic → NanumGothic` 순 대체. 기존 7개 업종 분석 로직은 그대로다.
- **팀원 산출물 백업**: `km_screening.py` 재실행으로 `output/4_*`, `4c_*`, `4d_*`가 새 데이터 기준으로 바뀌었다. 원본은 `output/_teammate_backup/`에 보관.
- 모든 스크립트는 `final_joined.csv`가 바뀌었으므로(행 −310, 컬럼 42개) 최종 산출물은 확정 후 한 번씩 재실행해서 갱신해야 한다.
- **`output/` 정리(74개 → 10개 항목)**: 팀원의 옛 버전(v1/v2, 이전 프랜차이즈 목록·이전 데이터) 산출물 중 재생성 가능하거나 낡은 것을 삭제했다 —
  실행 로그 9개, 편의점 안전판·`s4only` 변형 실행 결과, 중복 백업, 1·2·3단계 EDA/안전판 표(`eda.py` 등으로 재생성), `_v1_stale/`, 내 스텁 테스트 잔여물.
  **유지**: `5b_spatial_competitor.csv`(모델 입력), `5_경쟁밀도_시군구x업종.csv`(소진공 시군구 표), 새 데이터로 재생성한 KM(`4*`), `eda_candidates_oe.csv`.
  팀원의 v2(landmark) **최종 결과 13개와 KM 원본 4개는 `output/_baseline_v2_landmark/`로 옮겨** 재실행 전후(프랜차이즈 HR, BC 기여 등) 비교용으로 남겼다.
- **소진공 경쟁밀도(M4) 주의**: 5b는 소진공 2026-06 스냅샷 기준 반경 500m 동일업종 점포 수다. 스냅샷에는 추적 기간(1~6월)에 이미 폐업한 점포가 없어,
  폐업이 잦은 지역일수록 밀도가 낮게 측정되는 결과-의존 편향이 생긴다(현재 S1·S2 민감도에서 "누수"로 표기한 이유). 주 모형(M4)에 넣을 때는 이 한계를 명시하거나,
  1/1 시점에 영업 중이던 LOCALDATA 점포로 계산한 누수 없는 밀도를 함께 검토한다. 5b를 다시 만들려면 소진공 원본(`data/sojin_sanga/`)이 필요한데 이 컴퓨터에는 없다.

---

## ⑯ 팀원 인수인계 (2026-09-20)

- **넘어가는 것은 코드뿐이다(git)**: `preprocess_localdata.py`, `join_datasets.py`, `cox_rsf.py`, `km_screening.py`, 신규 `build_region_master.py`·`eda_candidates.py`,
  추적 대상이 된 `franchise_brands.csv`, 삭제된 `safety_net.py`. **CSV는 넘기지 않는다** — `final_joined.csv` 1.3GB 등은 GitHub 파일당 100MB 한도를 넘고, 원본이 같으면
  스크립트가 같은 결과를 재생성하므로 올려도 얻는 게 없고 오히려 코드와 어긋난 낡은 CSV가 남을 위험만 생긴다. `data/`·`output/`은 `.gitignore`에 넣었다.
- **원본 파일이 같은지 먼저 확인**(SHA-256 앞 16자리, 다르면 결과도 달라진다):
  `ABP_CONTEST_DATA.csv` 031c3a5ca96d4873 / `식품_일반음식점.csv` 63afcb4a0cd3d268 / `식품_제과점영업.csv` 32e93fdc827bf8bb /
  `식품_휴게음식점.csv` 6be3b78ed41adbaa / `생활_대규모점포.csv` 941bfd0cabc282e6
- **실행 순서**(전처리·조인을 먼저 다시 돌리지 않으면 옛 `final_joined.csv`에 새 컬럼이 없어 `cox_rsf.py`가 멈춘다):
  `preprocess_bc.py` → `preprocess_localdata.py` → `join_datasets.py` → (선택) `build_region_master.py`, `eda_candidates.py`, `eda.py`, `km_screening.py`.
  `5b_spatial_competitor.csv`는 기존 것을 그대로 쓴다(재실행 불필요).
- **정합성 확인용 숫자**(다르면 원본이나 코드 버전을 의심): 프랜차이즈 키워드 한글 145개·영문 4개, 매칭 138,927행(6.48%) / 날짜 정합성 제외 310행 /
  `localdata_wide` 2,144,806행×37컬럼, `localdata_clean` 27컬럼 / `final_joined` 2,144,806행×42컬럼, 조인 매칭률 99.99%(미매칭 255행) /
  `bc_group_month` 13,447행 / `region_master` 256개 / `cox_rsf.py` 로그의 `[C] 시작 2026-01-01 at-risk 635,567행 ... 폐업 25,330건 (3.99%)`, 그룹 1,782개·시군구 255개.
- **RSF 확인과 본 실행은 팀원 환경에서 한다**: 이 컴퓨터(Python 3.13)에는 `scikit-survival`이 설치되지 않아 RSF 경로(블록 정의를 바꿈)는 검증하지 못했다. 순서는
  1) 스모크 `python cox_rsf.py --biz 편의점 --n-boot 20 --n-diff-boot 20 --n-repeat 2 --rsf-n 20000 --rsf-trees 20 --n-perm 2`
  2) 업종 더미 경로 확인 `--biz 제과점 스넥 일식회집`(위와 같은 작은 옵션; Cox 쪽은 로컬에서 통과함)
  3) 본 실행 `python cox_rsf.py`(주 설계, 옵션 기본값) → 강건성 `python cox_rsf.py --design landmark` → 민감도 `--end 2026-09-16`.
  본 실행 전에 기존 `output/5_*.csv`(접미사 없는 v2 landmark 결과)는 baseline 폴더로 옮겨 새 결과(`..._cohort`, `..._landmark`)와 섞이지 않게 한다.
- **작업은 main에서**: 옛 `step5-group-time-split` 브랜치에서 그대로 작업하면 같은 파일(전처리·조인·CHANGELOG)에서 다시 충돌한다.

---

## ⑰ SHAP 분해(`explain_cox.py`)와 잔차 공간 점검 — 분석 정의와 1차 결과

### 분석 정의(결과 해석 전에 고정)
- **SHAP이 설명하는 대상**: Cox의 **선형예측자(log partial hazard)**. 시점과 무관한 위험도 점수이며 특정 시점의 생존확률이 아니다. Cox는 선형이라 사업장 i의
  변수 j 기여가 정확히 `계수_j × (x_ij − 평균_j)`이고 합하면 `(사업장 i의 선형예측자) − (평균 사업장의 선형예측자)`가 된다(실행 시 lifelines 예측값과 대조해 검증).
  해석은 곱셈 배수(평균 사업장 대비 상대위험)로 하고 "X% 기여"는 부호가 섞여 오해하기 쉬워 참고용으로만 쓴다. 연관의 분해이며 인과가 아니고, BC카드 변수는 그룹 단위 값이라
  생태학적 해석이다. 변수 하나하나가 아니라 **블록 단위**로 읽는다.
- **Moran's I용 잔차의 기준 모형**: 모형별로 따로 계산한다. 사업장별 (관측 폐업 − 기대 폐업 = 기저누적위험 × 위험도)를 시군구로 모은 값이다. **M3(경쟁밀도 없음)** 기준은
  "사업장 변수·지역 이력·BC카드로 설명한 뒤에도 공간 패턴이 남는가"를 본다. **M4(소진공 경쟁밀도)가 만들어지면 같은 검사를 반복**해, 경쟁밀도를 넣어도 공간 패턴이 남는지를
  비교한다(GNN 필요성 판단 근거). ※ 경쟁밀도를 주모형에서 "제외하기로" 한 것은 아니다 — M4로 넣되 결과 의존 편향 한계를 명시하거나 편향 없는 대안을 함께 검토한다(⑮).
- 구현: `explain_common.py`가 `cox_rsf.py`의 코호트 생성부를 **그대로 실행해서** 가져오므로(코드 복사 없음) 코호트 정의가 어긋나지 않는다. 위험집단 635,567행·폐업 25,330건과 다르면 중단한다.

### 1차 결과 (M3, 코호트 전체 적합, `output/6_explain_*.csv`)
- **분해 검증**: 사업장별 기여의 합과 lifelines 선형예측자의 최대 차이 2e-15 → 정확한 분해.
- **블록 중요도**(사업장 단위 SHAP 평균 절대값 비중): 영업연수 24.0%, 사업장 확장변수 20.8%, 프랜차이즈 18.2%, BC 연령 구성 16.3%, 업종 10.5%, 입지 3.7%, BC 성별 구성 3.3%, 지역 최근 폐업률 3.2%.
- **그룹(시군구×업종) 간 위험 격차를 가르는 요인**(블록 기여의 표준편차, log HR): BC 연령 구성 0.190 > 프랜차이즈 0.149 > 업종 0.104 > 영업연수 0.086 > 사업장 확장 0.078 > BC 성별 0.037 > 지역 최근 폐업률 0.035 > 입지 0.029.
- **분해 예시**: 화성 동탄구 서양음식은 평균 사업장의 1.85배 위험 = 영업연수 ×1.20 · 프랜차이즈 ×0.89 · 입지 ×1.01 · 사업장 확장 ×1.09 · 지역 최근폐업률 ×1.07 · BC성별 ×1.03 · **BC연령 ×1.66** · 업종 ×0.87.
  가장 안전한 그룹은 경남·전남·충북 군 지역 한식(0.50~0.59배)이고 여기서도 BC 연령 구성이 ×0.66~0.75로 가장 크다.
- **미학습 그룹 검증**(그룹 단위 분할, 그룹 평균 위험점수와 관측 그룹 폐업률의 Spearman, 사업장 ≥100인 그룹): M2 0.526 → M2h 0.556 → **M3 0.597**(학습 그룹은 0.511/0.540/0.574).
  학습→미학습에서 떨어지지 않아 과적합 징후는 없다. **(정정)** 이 단계에서는 "BC카드 블록이 처음 보는 그룹의 위험 순위를 개선한다"고 적었으나 점추정값일 뿐 신뢰구간이 없었고,
  ⑱에서 연령 블록의 증분을 시군구 cluster bootstrap으로 다시 보니 신뢰구간이 0을 포함했다. 이 문장은 근거가 충분하지 않았다. 사업장 단위 C-index 증가가 작은 것(+0.003~0.006)과 모순이 아니다 —
  C-index는 그룹 내 사업장 순위(영업연수·프랜차이즈)가 지배하고, BC는 그룹 간 변이만 다루기 때문. **한계**: 분할 1회, 미학습 그룹 212개, 신뢰구간 없음 → 반복 분할로 확인 필요.

### 해석 주의
- BC 연령 구성비 5개의 계수가 −6.7~−10.5로 크고 구성비 합이 1이라 서로 겹친다(기준 범주 = 연령코드 1의 비중 하나로 요약되는 패턴). **개별 계수 해석 금지, 블록 단위로만.**
- 연령 구성은 "고객 연령이 폐업을 만든다"기보다 신도시·도심 대 농촌 같은 **상권 유형의 대리 변수**일 가능성이 크다(가장 위험한 그룹이 화성 동탄·인천 서구 등 신도시, 가장 안전한 그룹이 군 지역).
- `지역 최근 폐업률`은 단변량에서는 강했지만(⑩, O/E 0.76~1.16) 조건부 효과는 작다(1SD당 HR 1.04) — BC 변수와 그룹 수준 변이를 나눠 갖는다.

---

## ⑱ BC 연령 구성은 상권 유형의 대리 변수인가 — 통제·층화 검정 (`confound_check.py`)

- **동기**: ⑰에서 BC 연령 구성이 그룹(시군구×업종) 간 위험 격차를 가르는 1위로 나왔으나, 가장 위험한 그룹이 신도시·가장 안전한 그룹이 군 지역이라 상권 유형의 대리 변수일 수 있다.
  이 블록 분산의 42%만 시군구 간 차이이고 **58%는 같은 시군구 안의 업종 간 차이**라, 시군구 수준 변수 하나(예: 점포 수)를 통제하는 것만으로는 검정이 안 된다.
- **설계**: M3(기준) / M3c(+구·군 행정구역 유형 + 소진공 음식·편의 상가 수 로그) / **M3s(시군구별 층화 — 시군구 수준의 모든 교란을 측정 없이 제거)**. 평가는 SHAP 비중이 아니라
  (1) 연령 블록 Wald 통계량의 모형 간 감쇠, (2) 미학습 그룹 순위상관(Spearman)에서 연령 블록을 뺐을 때의 증분과 시군구 cluster bootstrap 신뢰구간(B=500).
- **방법 정정**: lifelines의 군집-강건 분산은 동률(ties)을 처리하지 못한다고 문서에 명시돼 있고(일 단위 기간이라 동률이 많음) 실제로 오류도 나서 쓰지 않았다. Wald는 일반 분산이라
  사업장을 독립 관측으로 세어 유의성이 부풀려지므로 **p값은 쓰지 않고 상대 비교로만** 본다. 추론은 (2)의 bootstrap으로 한다.
- **결과 1 — 통제하면 효과가 줄어든다**(그룹 간 기여 표준편차, Wald): M3 0.190(247.9) → M3c 0.109(88.2, −43%) → M3s 0.111(24.1, −42%). 계수 자체가 약 40% 줄었다.
- **결과 2 — 미학습 그룹 순위 증분**(212개 그룹, 139개 시군구; 연령 블록의 ΔSpearman [95% CI]):
  | 기준 | M3 | M3c(+통제) | M3s(층화) |
  |---|---|---|---|
  | 전체 그룹 | +0.041 [−0.007, +0.086] | +0.025 [−0.004, +0.054] | — |
  | 같은 시군구 내 편차 | +0.012 [−0.038, +0.086] | +0.001 [−0.047, +0.050] | −0.026 [−0.108, +0.054] |
  **모든 신뢰구간이 0을 포함한다.** 특히 결정적인 검정인 시군구 내 편차 기준에서 연령 블록의 증분은 사실상 0이다.
- **해석**: BC 연령 구성이 상권 유형과 무관한 독립 효과를 가진다는 근거는 이 데이터에서 찾지 못했다. 동시에 작은 효과를 배제하지도 못한다(미학습 그룹 212개라 신뢰구간이 ±0.05~0.09로 넓음).
  현재 증거는 "상권 유형의 대리 변수"에 더 가깝다. 통제 변수(구·군 유형+상가 수) 자체는 예측을 개선했다(전체 그룹 Spearman 0.597 → 0.622, 신뢰구간 미계산).
- **한계**: 분할 1회, 통제가 완전하지 않음(층화 모형만 시군구 수준을 전부 제거), 연령 구성비 5개는 서로 겹쳐 개별 계수 해석 불가.
- **정정**: ⑰에서 "BC카드 블록이 미학습 그룹의 위험 순위를 개선한다"고 쓴 것은 점추정값만 보고 한 과한 해석이었다(위 ⑰에 정정 표시).
- **아직 안 본 것**: 팀원 모형의 BC 변수는 성별·연령 **구성비**뿐이고 **소비 규모·점포당 소비액은 쓰지 않았다.** 프로젝트의 핵심 질문("소비는 있는데 왜 못 버티는가")에 더 직접적인 변수라 검토가 필요하다.

---

## ⑲ M4(경쟁밀도·상권 규모)·M5(BC 소비 규모) 검증 — 그룹/시군구 5-fold (`bc_scale_check.py`)

- **설계**: ⑱의 단일 분할(미학습 212개 그룹)을 **그룹 5-fold**(평가 그룹 1,090개, 시군구 255개)와 **시군구 5-fold**(미학습 시군구 외삽)로 바꿨다. fold별 모형이 미학습 그룹에 낸
  선형예측자를 모아 그룹 평균 위험점수 vs 관측 폐업률의 Spearman을 보고, 시군구 cluster bootstrap(B=500)으로 증분의 신뢰구간을 낸다. 시군구 층화 모형은 미학습 시군구를 예측할 수 없어 그룹 fold에서만 평가.
- **새 변수**(전부 1/1 이전 정보 또는 1월 소비): M4 = 그룹 1/1 영업 점포 수(로그), M4v = +시군구 1/1 영업 점포 수(9업종, 외식·대규모점포만 포함)·500m 밀집도,
  M5J = +1월 점포당 소비·객단가(업종 내 z), M5S = 6개월 월평균/1/1 점포 수, M5D = 월별 (소비/그달 1일 점포 수)의 평균. ABP에 점포별 매출이 없어 "폐업점포 매출 제외" 재계산은 불가능해 M5D로 대체.
- **방법 오류 정정(중요)**: 첫 실행은 fold별 선형예측자를 중심화하지 않고(β'x) 모았다. 연령 구성비처럼 평균이 큰 변수는 fold마다 계수가 조금만 달라도 그룹 점수에 fold별 상수가 실려
  순위가 무너졌다(연령 블록을 빼면 Spearman이 0.236→0.361로 "좋아지고" 시군구 fold에서는 0.061→0.475로 폭증하는 비정상 결과). 학습 평균으로 중심화(β'(x−평균))해서 다시 돌렸고, 이 표의 값은 재실행 결과다.
  (사업장 단위 C-index도 0.584→0.635로 같은 원인이 사라졌다.) 검증이 이상한 방향이면 모형이 아니라 평가 코드를 먼저 의심해야 한다는 사례.
- **결과 — 미학습 그룹 순위(ΔSpearman, [95% CI])**
  | 블록 | 그룹 5-fold 전체 | 그룹 5-fold 시군구 내 | 시군구 5-fold 전체 |
  |---|---|---|---|
  | BC 연령 구성(M3 − 연령 뺀 M3) | **+0.034 [0.008, 0.061]** | +0.004 [−0.015, 0.021] | **+0.032 [0.006, 0.058]** |
  | 경쟁밀도(M4 − M3) | +0.002 [−0.003, 0.007] | −0.004 [−0.008, 0.000] | +0.001 [−0.004, 0.006] |
  | 시군구 규모·밀집도(M4v − M4) | +0.005 [−0.005, 0.017] | +0.013 [0.000, 0.026] | −0.001 [−0.012, 0.012] |
  | 소비 1월(M5J − M4) | −0.001 [−0.010, 0.007] | −0.005 [−0.015, 0.005] | −0.005 [−0.014, 0.003] |
  | 소비 6개월 월별 분모(M5D − M4) | −0.000 [−0.010, 0.008] | −0.004 [−0.014, 0.008] | −0.005 [−0.014, 0.004] |
  시군구 층화 모형의 연령 블록 증분: −0.004 [−0.024, 0.012]. 사업장 단위 C-index(미학습 예측 모음)는 M3 0.6351 / 연령 뺀 M3 0.6330 / M4·M5 모두 0.635.
- **해석**
  1. **소비 규모(M5)는 순위를 개선하지 못했다.** 어느 기준에서도 증분이 0 근처이고 신뢰구간이 ±0.01 정도라, +0.01 이상의 개선은 배제된다. 1월 단독과 6개월 버전의 결과도 같아 역인과 왜곡이 결론을 좌우하지 않는다.
     전체 적합의 소비 계수는 존재하지만(1SD당 HR 1.03~1.04) 일반 SE로 계산해 유의성이 부풀려진 값이고, 예측 개선으로는 이어지지 않는다.
  2. **경쟁밀도(M4)도 개선이 없다.** 시군구 규모·밀집도(M4v)는 시군구 내 편차에서만 경계선상(하한 0.000)인데, 이 변수는 시군구 안에서 상수라 다른 계수의 재조정 효과일 뿐 해석하지 않는다.
  3. **BC 연령 구성은 시군구 간(전체 그룹) 순위에는 작게(+0.03) 기여하지만 같은 시군구 안 순위에는 기여하지 않는다**(층화 포함). ⑱과 일치하며, 신뢰구간이 좁아진 만큼 결론이 더 분명해졌다:
     연령 구성은 "어떤 지역인가"를 알려 주는 지역 유형 지표로는 유용하지만, 같은 지역에서 업종별 고객 연령이 위험을 가른다는 근거는 없다.
  4. **프로젝트 주제에 대한 함의**: 현재 데이터에서 BC카드 소비 규모·구성은 사업장 특성(영업연수·프랜차이즈·입지)과 지역 최근 폐업률을 넘는 설명력을 거의 주지 못한다.
     "소비가 있는데도 못 버티는 이유"는 소비 수준이 아니라 사업장·지역 요인 쪽에 있다는 **부정적 결과**를 정직하게 보고하는 것이 맞다. (M3와 지역 폐업률만 있는 M2h의 5-fold 비교는 아직 안 돌렸다.)
- **추가 검증 1 — 모형 사다리**(그룹 5-fold 전체 그룹 Spearman / 사업장 C-index): M2 0.501 / 0.621 → M2h 0.534 / 0.633 → M3 0.562 / 0.635.
  지역 최근 폐업률(M2h−M2) +0.032 [0.005, 0.060]로 **0 제외**, BC 성별·연령 전체(M3−M2h) +0.029 [−0.001, 0.056]로 **경계선(0 포함)**. 시군구 fold도 같은 방향(+0.034, +0.026).
  같은 시군구 안 순위는 모든 모형이 0.45~0.47로 거의 같다 — 시군구 내 순위는 업종·영업연수·프랜차이즈 같은 사업장 변수가 거의 다 만든다.
- **추가 검증 2 — 아직 안 뽑아 본 BC 특성 7개(M6)**: 연령 집중도(HHI), 지역 평균 대비 업종 고객 연령·여성비 편차, 지역 점포당 소비력, 지역 소비 중 업종 점유, 1→6월 성장률, 월 변동계수.
  M6 − M4 = −0.002 [−0.015, 0.011](그룹 fold 전체), −0.007 [−0.019, 0.007](시군구 fold). 층화(M6s)에서도 −0.012 [−0.031, 0.003]. 개선 없음.
  ABP 원자료는 월·시군구·성별·연령·업종·금액·건수 7개 항목뿐이라, 이 자료에서 만들 수 있는 특성은 사실상 다 시험한 셈이다.
  (층화 모형에서는 시군구 수준 변수와, 층 안에서 연령 구성비의 선형결합이 되는 편차 변수를 빼야 식별된다 — 그대로 넣으면 특이행렬 오류.)
- **한계**: 그룹 단위 폐업률은 사업장 수가 적으면 잡음이 크고(n>=100 그룹만 평가), Spearman은 큰 격차가 아닌 순서만 본다. 소비 변수는 그룹 단위 생태학적 변수이며 6개월 관측창 하나만 있다.
  fold 분할은 시드 42 한 번(신뢰구간은 시군구 bootstrap이지 분할 반복이 아님).
- 산출: `output/8_group_features.csv`, `8_cv_group_*`, `8_cv_region_*`, `8_fullfit_new_vars.csv`, `8_cv_group_cindex.csv`.

---

## ⑳ 위험 원천 분해와 "소비는 있는데 못 버티는" 그룹 분석 (`risk_decomp.py`)

- **분석 1-a (M3 위험점수 분산의 블록별 몫, 그룹 1,090개)**: BC 연령 44%, 영업연수 20%, 사업장 확장 13%, 지역 폐업률 11%, BC 성별 9%, 입지 6%, 프랜차이즈 2%, 업종 −5%.
  그룹 간 분산의 61%가 시군구 간, 39%가 시군구 내. **주의: 이 분해는 전체 코호트 in-sample 값이다.** BC 변수는 그룹 단위 값이라 유효 표본이 약 1,800개인데 계수 7개가 그룹 잡음까지 맞출 수 있고,
  실제로 시군구 내 분산의 29%가 연령 구성 몫으로 잡히지만 5-fold 검증(⑲)에서 같은 시군구 안 순위 증분은 0이었다. **BC 몫은 과대일 가능성이 높아 "위험의 44%가 고객 연령 때문"으로 발표하면 안 된다.**
- **분석 1-b (조정 R², 사업장 수 가중)**: 관측 폐업률 — 업종만 0.12, 시군구만 0.44, 둘 다 0.57(이항 우연변동이 분산의 28%라 상한은 약 72%). 모형 위험점수 — 업종만 0.28, 시군구만 0.61, 둘 다 0.87.
  **그룹 간 폐업률 차이에서 "어느 지역이냐"가 "어느 업종이냐"보다 3~4배 크다.** (시군구 더미 255개를 그룹 1,090개에 적합한 조정 R²라 다소 낙관적이다.)
- **분석 3 (M2h 기준 초과 폐업 = 관측 − 기대, 교정 확인: 기대 3.98% vs 관측 3.99%)**
  - **원자료에서 소비가 많은 그룹이 더 많이 폐업한다**(점포당 소비 1월 업종 내 3분위 관측 폐업률: 하 3.30% / 중 4.12% / 상 4.30%, 순위상관 +0.19 [0.13, 0.26]). "소비가 많으면 버틴다"는 직관과 반대.
    다만 같은 시군구 안에서는 관계가 없다(+0.02 [−0.04, 0.08]) — 시군구 간 차이(도심·고회전 상권에서 소비도 폐업도 높음)다.
  - 사업장·지역 요인을 반영한 초과분과는 약하게 연관(1월 +0.107 [0.029, 0.180], 초과 폐업률 상 +0.12%p / 하 −0.36%p, 기대 폐업률의 10% 안팎). 6개월 소비는 시군구 내에서도 +0.079 [0.023, 0.139]로 나오나
    **월별 분모(영업 점포 수)를 쓰는 구조상 폐업이 늘면 점포당 소비가 기계적으로 오르므로 신뢰하지 않는다.** 1월 소비(분모가 1/1로 고정)는 시군구 내에서 유의하지 않다(+0.052 [−0.007, 0.112]).
  - **객단가가 높은 그룹은 폐업이 적다**(관측 폐업률 하 4.25% / 상 3.50%, 순위상관 −0.176 [−0.249, −0.107], 초과 기준 −0.085 [−0.159, −0.022]), 시군구 내에서는 관계 없음.
  - **고소비인데 기대보다 폐업이 많은 그룹**(소비 상위 3분위 365개 중 z>1.96 44개, 12%): 관측 7.3% vs 기대 4.4%. 지역 폐업 이력·영업연수·다중이용업소 비중은 나머지 고소비 그룹과 같고,
    **프랜차이즈 비중이 8%(vs 22%)로 낮고 점포 수가 많다**(평균 1,059개 vs 610개). 서울·광역시 도심의 한식계열·서양음식·스낵이 다수(광진구 서양음식, 대구 남구 한식 등).
    => "개인 점포가 밀집한 대형 도심 상권"이라는 가설을 세울 수 있으나, 프랜차이즈 효과가 이미 M2h에 사업장 단위로 들어 있는데도 남는 초과분이므로 **그룹 수준 프랜차이즈 비중(맥락 효과)을 새 후보 변수로 5-fold에서 검증**해야 한다(미실시).
  - z>1.96 비중이 하위 그룹에서도 6%로 우연 기대(2.5%)보다 커, 그룹 내 폐업의 상관(공통 충격)으로 z가 과대 분산이다 — 순위·후보 선정용이지 유의성 검정이 아니다.
- **종합**: BC 소비 규모는 폐업을 "막아 주는" 변수가 아니고(오히려 도심 고회전 상권과 함께 움직임), 같은 시군구 안에서는 폐업 순위를 가르지 못한다. 위험의 큰 축은 지역이고, 지역 안에서는 사업장 특성이 대부분을 만든다.
- 산출: `output/9_variance_decomp_blocks.csv`, `9_region_vs_biz_r2.csv`, `9_excess_by_spend_tier.csv`, `9_excess_spend_spearman.csv`, `9_high_spend_high_excess.csv`, `9_high_spend_composition.csv`.

---

## ㉑ 그룹 구성(맥락 효과) 검증 M7, 잔차의 공간 자기상관 Moran's I (`bc_scale_check.py` 확장, `moran_residuals.py`)

- **M7 = M4 + 그룹 구성(같은 시군구×업종 1/1 점포의 프랜차이즈 비중·평균 영업연수·다중이용업소 비중)**. ⑳의 "개인 점포 밀집 대형 상권" 가설 검증.
  - 전체 적합에서 그룹 평균 영업연수 계수가 매우 크다(−0.61, 일반SE z −14.5; 상대 지표) — 그룹 안에 오래된 점포가 많으면 개별 영업연수를 통제해도 위험이 낮다. 프랜차이즈 비중(z 0.7)·다중이용(z −0.4)은 미미.
  - 5-fold: 전체 그룹 Spearman 0.564 → 0.580(M7 − M4 +0.016 [−0.006, 0.039], 0 포함), 시군구 fold +0.019 [−0.005, 0.045]. **같은 시군구 안 순위는 오히려 나빠진다**(−0.034 [−0.057, −0.009], 시군구 fold −0.029 [−0.054, −0.004]; 층화 −0.008 [−0.021, 0.003]).
    사업장 C-index 0.635 → 0.637. => **개인 점포 밀집 가설(프랜차이즈 비중 맥락 효과)은 지지되지 않는다.** 그룹 평균 영업연수가 시군구 간 순위를 약간 돕는 것으로 보이나 신뢰구간이 0을 포함하고 시군구 내에서는 역효과라 채택하지 않는다.
- **Moran's I(시군구 255개, Pearson 잔차, 순열 5,000회, k=5)**: 원자료 폐업률 0.430 → M2 0.299 → M2h 0.248 → M3 0.167(in-sample) / 0.157(OOF, 미학습 예측). k=10도 같은 순서(0.341 → 0.222 → 0.163 → 0.096). 모든 값이 p<0.002.
  - **모형이 지역 구조를 상당 부분(0.43 → 0.16) 흡수했지만 잔차에 유의한 공간 군집이 남아 있다.** BC 변수가 들어간 M3에서 M2h(0.25)보다 낮아지는 것은 BC 연령 구성이 지역 유형 정보를 실어 나르기 때문으로 ⑱과 일치한다(OOF에서도 같음, 0.241 → 0.157).
  - **업종별**: 한식계열 0.19(z 5.2, 시군구 251개)가 주도. 서양음식 0.07~0.09(경계선), 스낵·일식회집·편의점은 0 근처. 잔차의 공간 군집은 사실상 한식계열 현상이다.
  - **군집 위치(M3 OOF, 국지 I)**: 예상보다 폐업이 많은 군집은 서울 동북부·경기 동북부 벨트(구리·파주·일산동구·강동·하남·덕양·남양주·도봉·광진·노원). 적은 군집은 경북·강원·전북의 중소도시(김천·속초·경주·군산·강릉·영주·구미·안동)와 도심(서울 중·종로, 부산 중구).
  - 주의: 시군구 잔차 z는 그룹 내 폐업의 상관 때문에 과대 분산이라 개별 z는 순위용이고, Moran's I는 표준화한 값끼리의 상관이라 이 영향이 작다. 중심점은 좌표 중앙값, 가중은 kNN이라 섬(제주·울릉·옹진)의 이웃은 부정확하다.
- **다음 판단(GNN 여부)**: 공간 구조가 남아 있으므로 지리를 더 살릴 근거는 있다. 다만 GNN은 설명가능성(SHAP 분해)과 충돌하고 데이터(255개 노드)가 작으므로, **먼저 이웃 시군구의 1/1 이전 폐업 이력(공간 지연 변수)을 M2h에 더해 Moran's I가 줄고 5-fold 순위가 오르는지** 보고, 그래도 남으면 GNN을 검토한다(미실시).

---

## ㉒ 공간 지연 변수(이웃 시군구의 1/1 이전 폐업 이력) — GNN 전 단계 검증 (`bc_scale_check.py`, `moran_residuals.py`)

- **변수(모두 1/1 이전 1년 폐업 이력만, 사전 결정)**: reg_hist = 자기 시군구 전체(7업종) 직전 1년 폐업률, nb_reg_hist = 시군구 중심점 k=5 최근접 이웃의 같은 지표(1년 전 점포 수 가중 평균),
  nb_grp_hist = 이웃 시군구의 같은 업종 그룹 직전 1년 폐업률(점포 수 가중, 이웃에 그 업종이 없으면 nb_reg_hist로 대체). M2hR = M2h + reg_hist, M2hL = M2hR + 이웃 2개, M3L = M3 + 세 변수.
- **5-fold 순위(ΔSpearman, [95% CI])**: 자기 시군구 이력만 추가(M2hR − M2h) +0.006 [−0.019, 0.030]로 효과 없음. **이웃 이력 추가(M2hL − M2hR) +0.030 [0.012, 0.053], 합계(M2hL − M2h) +0.036 [0.008, 0.067],
  M3 기준(M3L − M3) +0.031 [0.005, 0.057]**(시군구 fold도 +0.027 / +0.031 / +0.029, 모두 0 제외). 같은 시군구 안 순위는 변화 없음(시군구 수준 변수이므로 당연). 사업장 C-index M3 0.6351 → M3L 0.6371.
  전체 적합 계수는 nb_reg_hist가 크고(z 8.7) nb_grp_hist는 미미(z 0.4) — 정보는 "이웃 지역 전반의 폐업 이력"에서 온다.
- **Moran's I(잔차, 미학습 예측 OOF, k=5 / k=10)**: M2h 0.241 / 0.155 → M2hR 0.154 / 0.090 → M2hL 0.077 (p=0.016) / 0.031 (p=0.09) → **M3L 0.023 (p=0.23) / −0.005 (p=0.49)**.
  M3(0.157)에서 M3L로 공간 자기상관이 통계적으로 사라진다. (kNN 가중과 이웃 정의가 같아 어느 정도 구조적으로 줄어드는 면이 있으나 k=10에서도 사라진다.)
- **결론**: 남아 있던 공간 군집은 "이웃 지역의 최근 폐업 흐름"이라는 해석 가능한 변수 하나로 거의 흡수된다. **GNN은 필요하지 않다**(설명가능성과 충돌, 노드 255개로 작음). 최종 모형 후보는 **M3L**(사업장 변수 + 지역 이력 + 이웃 이력 + BC 성별·연령).
  BC 소비 규모(M5)·경쟁밀도(M4)·그룹 구성(M7)은 채택하지 않는다(⑲, ㉑). 남은 절차: 최종 모형(M3L)으로 SHAP 분해를 다시 내고(이웃 이력 블록 추가), 정적 리포트로 정리.
- **한계**: 이웃 이력은 이웃 지역의 '이전 1년' 폐업 흐름이라 원인이 아닌 지역 국면(경기·개발 등)의 대리 변수다. 시군구 중심점 kNN은 섬(제주·울릉·옹진) 이웃이 부정확하다. 분할 1회.

---

## ㉓ 최종 모형 M3L의 설명: SHAP 분해 + 블록 제거 예측 중요도 (`explain_final.py`)

- **M3L** = 사업장 변수(영업연수·프랜차이즈·입지·사업장 확장) + 그룹 직전 1년 폐업률 + 자기·이웃 시군구 폐업 이력 + BC 성별·연령 구성 + 업종. 분해는 정확하다(오차 1.2e-15).
- **SHAP 크기(사업장 단위 평균 절대 기여, in-sample)**: 영업연수 23%(전형적 ×1.23), 사업장 확장 21%, 프랜차이즈 19%, BC 연령 13%, 자기·이웃 폐업 이력 11%, 업종 7%, 입지 3%, BC 성별 2%, 그룹 직전 폐업률 0.7%.
- **블록 제거 예측 중요도(그룹 5-fold OOF, M3L에서 블록을 뺐을 때의 하락)** — SHAP 크기와 순서가 다르다. M3L 전체: 그룹 Spearman 0.594, 시군구 내 0.455, C-index 0.6371.
  | 블록 | ΔC-index | ΔSpearman 전체 [95% CI] | ΔSpearman 시군구 내 [95% CI] |
  |---|---|---|---|
  | 영업연수 | **0.0127** | 0.016 [0.006, 0.027] | 0.006 [−0.007, 0.020] |
  | 사업장 확장 | 0.0095 | 0.007 [−0.006, 0.022] | 0.004 [−0.008, 0.016] |
  | 프랜차이즈 | 0.0087 | 0.001 [−0.008, 0.008] | 0.008 [−0.007, 0.021] |
  | 자기·이웃 시군구 폐업 이력 | 0.0021 | **0.031 [0.002, 0.057]** | −0.001 [−0.014, 0.015] |
  | BC 연령 구성 | 0.0014 | **0.025 [0.007, 0.046]** | 0.007 [−0.010, 0.022] |
  | 업종 | 0.0012 | 0.018 [−0.003, 0.039] | **0.049 [0.009, 0.083]** |
  | 입지(중심점 거리) | 0.0001 | 0.001 | 0.000 |
  | 그룹 직전 1년 폐업률 | 0.0000 | 0.001 | 0.002 |
  | BC 성별 구성 | −0.0001 | −0.002 | **−0.007 [−0.013, −0.002]** (뺐을 때 오히려 개선) |
- **읽는 법**: (1) 사업장 단위 판별력(C-index)은 영업연수·사업장 확장·프랜차이즈가 만든다. (2) 그룹 간(지역 간) 순위는 자기·이웃 폐업 이력과 BC 연령이 만든다 — 후자는 ⑱에서 확인했듯 지역 유형의 대리 변수다.
  (3) 같은 시군구 안 순위는 업종이 만든다. (4) 입지·그룹 직전 폐업률·BC 성별은 예측에 필요하지 않다(간소화 후보; 이번에는 모형을 바꾸지 않았다).
  BC 연령의 SHAP 몫(13%)은 블록 제거 기준으로는 시군구 간 순위(+0.025)에만 기여해 과대하다.
- **대표 그룹(사업장>=300, 상대위험 배수)**: 가장 위험 — 서울 마포구 제과점 ×1.91, 화성 동탄구 서양음식 ×1.90·한식계열 ×1.85, 인천 중구 서양음식 ×1.76, 대구 중구 서양음식 ×1.75, 송파구 서양음식 ×1.72.
  가장 안전 — 경남 합천군 한식계열 ×0.44, 산청군 ×0.46, 하동군 ×0.48, 거제시 일식회집 ×0.51, 창녕군 ×0.53(영업연수·이웃 폐업 이력이 낮음). 배수는 평균 사업장 대비 연관이며 인과가 아니다.
- 산출: `output/11_final_coef.csv`, `11_final_block_importance.csv`, `11_final_drop_block.csv`, `11_final_group_blocks.csv`, `11_final_region_blocks.csv`.

---

## ㉔ 정적 리포트 (`build_report.py` -> `report/index.html`)

- 외부 라이브러리·CDN 없는 단일 HTML(약 35KB, 다크 모드·모바일 대응). 수치는 전부 `output/`의 CSV에서 읽어 생성하므로 분석을 다시 돌리면 `python build_report.py`로 갱신된다.
- 구성: 핵심 결론 5개 → 블록 제거 예측 중요도 → 지역 vs 업종 → BC 소비 데이터가 말해 주는 것/못 말해 주는 것(시도했으나 채택하지 않은 변수의 음의 결과 표 포함) →
  위험/안전 그룹 8개씩 요인별 배수 표 → 공간 구조·이웃 폐업 이력 → BC카드 시사점 → 데이터와 한계.
- 발표 프레임은 "소비 규모가 아니라 점포 성격과 지역의 최근 폐업 흐름"이며, 모든 표현은 연관이다. 결과 변수·BC 변수의 해상도 차이와 6개월 단일 관측 창을 한계로 명시했다.
- 발행(공유 링크)은 하지 않았다 — 공모전 제공 데이터(ABP)의 재배포 조건을 확인한 뒤 결정.

---

## ㉕ RSF 독립 평가와 SHAP 분해의 RSF 근사·Cox 비교 (2026-09-20, ⑰·㉓과 병행한 별도 구현)

`rsf_eval.py`, `shap_rsf_approx.py`, `cohort_common.py`를 추가했다(코호트·M3 정의는 `cox_rsf.py`와 같고 위험집단 635,567행·폐업 25,330건 일치). **Cox 정확 SHAP 분해는 팀원의 ⑰·㉓(`explain_cox.py`, `explain_final.py`)을 기준으로 삼고, 겹치던 내 구현(`shap_decomposition.py`)은 삭제했다.** 이 항목은 팀원 쪽에 없는 것만 다룬다: ① RSF 독립 평가, ② RSF 블록 단위 SHAP 근사와 Cox 정확 SHAP의 비교.

### RSF 독립 평가 (`rsf_eval.py`)
- **설정**: `cox_rsf.py`의 RSF(100트리, 고정 하이퍼파라미터)와 달리 튜닝·다지표 평가를 체크리스트대로 수행. 주 설계 코호트(1/1 영업 중 → 6/30) n=635,567, 폐업 25,330건(⑯ 기준값과 일치), 검열 96.01%, 그룹 1,785개
  (`cox_rsf.py`의 1,782개보다 3개 많은 건 그쪽이 공변량이 빈 31행을 삭제하기 때문. 여기서는 Pipeline의 중앙값 대체).
- **분할**: `StratifiedGroupKFold`(event stratify + 시군구×업종 그룹 통째 분리) → train 503,066행(폐업 20,231) / test 132,501행(폐업 5,099). 행 단위 stratify는 그룹 변수가 새서 쓰지 않았다.
- **튜닝**: RandomizedSearchCV 12조합 × 3폴드, 4만 행, 500트리, 기준 Harrell C. `min_samples_leaf`는 리프당 기대 폐업 15/30/60건에 해당하는 370/740/1480.
  최적 `leaf=370, split=1480, max_features=sqrt, max_depth=None`(CV C 0.6237 ± 0.0149). **상위 8개 조합이 0.6218~0.6237로 0.002 안에 붙어 조합 간 우열을 가릴 수 없다**(폴드 표준편차 0.011~0.015). 큰 그림은 "나쁜 조합 배제" 수준.
- **최종 모형**: 15만 행 무작위 추출 × 500트리(전체 train 50만 행이 아님). 소요 145분.

| 지표 (test, 그룹 hold-out) | 값 |
|---|---|
| OOB C-index (행 단위, 오차 아님) | 0.6463 |
| Harrell C | **0.6450** (그룹 부트스트랩 95% CI 0.6356~0.6555) |
| Uno C (tau=172일) | 0.6441 |
| 업종 내 Harrell C | 0.6388 |
| 평균 시간가변 AUC (8~163일) | 0.6410 |
| IBS / KM 기준선 IBS | 0.0188 / 0.0190 (개선 0.7%) |

- **Uno ≈ Harrell인 이유**: 검열이 전부 추적 종료일(180일)에 몰린 행정적 검열이라 그 전 구간에서 censoring 생존확률이 1이고 IPCW 가중치가 모두 1이다. Uno·AUC(t)·Brier의 IPCW 효과는 사실상 없다.
- **IPCW 지표의 절단**: 마지막 날(180일) 사건에서 censoring 생존확률이 0이라 오류가 나므로 AUC(t)·Brier·IBS는 test를 175일에서 절단해 계산(Uno는 tau=172). **마지막 약 8일의 사건은 이 지표들에 들어가지 않는다.**
- **변별력은 중간(약 0.645), 확률 정확도 개선은 작다**: 폐업이 4%로 드물어 Brier가 기저 폐업률에 지배된다(Brier skill 172일 1.06%).
- **Calibration**: 전체 평균 예측과 KM은 거의 겹친다. 십분위에서 저위험 하위 2~3분위는 과대예측(172일 예측 2.2%·2.6% vs 실제 1.7%·2.0%), 최상위는 과소예측(7.09% vs 8.02%)이라 위험도의 양극단을 약간 덜 벌린다. 추적이 180일뿐이라 1년·3년 calibration은 정의되지 않아 86일·172일로 봤다.
- **permutation importance (test 2만 행, 10회, C-index 감소)**: 영업연수 0.060 ≫ 프랜차이즈 0.014 > 시설 크기 블록 0.008 > BC 전체 블록 0.0125(연령 0.0078, 성별 0.0057) ≈ 지역 최근 폐업률 블록 0.005. BC 구성비는 서로 강하게 종속이라 개별로 섞으면 과소평가되고(개별 합 약 0.007) 블록으로 섞어야 0.0125가 나온다.
- **한계**: 시간가변 공변량은 다루지 못해 BC 월별 값을 1~6월 합계 정적 변수로 요약했다(추적 기간 중 값을 시작 시점에 붙인 것이라 역인과 가능성). 경쟁위험은 없다(사건은 폐업뿐).

### RSF 블록 SHAP 근사와 Cox 비교 (`shap_rsf_approx.py`, 공용 `cohort_common.py`)
- **RSF 근사**: RSF에는 정확한 SHAP 공식이 없어 블록 단위 permutation-sampling Shapley를 쓴다(플레이어 = 10개 의미 블록, 값 함수 = log 위험점수, 배경 = train 사업장).
  위험 상위 20·하위 10·무작위 30 그룹(Cox 로그위험 순위 기준, 같은 업종 평균 대비)에서 사업장 1,500개만 설명했다. 200트리·10만 행으로 다시 학습한 모형(test C 0.6476). 가법성 오차 7.8e-16, 블록 φ의 평균 표준오차 0.015.
- **비교 대상 Cox**: 이 스크립트가 train에서 직접 적합한 Cox M3(test Harrell C 0.6364)의 정확 SHAP. 팀원 `explain_cox.py`와 같은 정의이며(기준 = train 평균 사업장) 블록 비중도 같은 수준이다(BC 연령 14.5% vs 팀원 ⑰ 16.3%).
- **Cox와 RSF의 일치도**(같은 1,500개): 사업장 총 기여 상관 0.905, 영업연수 1위 일치. 순위가 다른 곳: 프랜차이즈 Cox 4위/RSF 6위, 업종 5위/9위, 지역 최근 폐업률은 RSF에서 더 큼. BC 연령 구성의 평균 |φ|는 Cox 0.206, RSF 0.098로 RSF가 절반 수준이다.
- **⚠ 해석 주의**
  1. BC 연령 구성 계수는 −9.2~−12.4로 비슷해 합=1인 구성비의 준거범주 비중 하나로 요약되고, 팀원 ⑱의 통제·층화 검정에서 연령 블록의 미학습 그룹 순위 증분 신뢰구간이 모두 0을 포함했다. 이 블록의 SHAP 비중은 상권 유형의 대리 변수로 읽는다. RSF에서 BC 연령이 영업연수의 약 1/6(permutation)으로 나온 것과도 모순이 아니다(SHAP은 예측을 움직이는 양, permutation은 순위 정확도를 올리는 양).
  2. 모형 기여이지 인과가 아니다. RSF 근사는 선택한 60개 그룹만 설명했고 위험 극단 그룹을 포함해 평균 |φ|가 전체 사업장 기준보다 크다.
  3. 위험 상위 그룹에서 인천 서구·중구가 "입지(중심지 거리)"로 크게 나온 원인은 확인하지 못했다. 검단구·영종구 분리로 지역 라벨이 섞인 곳이라 입지 변수가 왜곡됐을 가능성이 있어 확인 전까지 해석을 보류한다.

### 재현
- 환경: Python 3.9.6, scikit-learn 1.5.2, scikit-survival 0.23.1, lifelines 0.30.0, seed 42.
- `python3 -u rsf_eval.py`(약 145분) → `output/rsf_eval_*`. `python3 -u shap_rsf_approx.py`(약 10분, 다른 스크립트 산출물 불필요) → `output/shap_rsf_*`.
- **재현 확인**: `shap_rsf_approx.py`는 Cox 적합을 안에 넣는 수정 뒤 다시 돌려 이전 실행과 비교했다. 블록별 평균 |φ|(RSF·Cox)가 0.001 이내로 같고 RSF 순위 10개가 모두 동일, 가법성 오차 7.8e-16 유지. 설명 대상 60개 그룹 중 2개만 달랐는데(위험 상위 20·하위 10은 동일, 무작위 30개 중 2개) 이전 실행이 순위표를 소수 5자리로 반올림한 CSV에서 읽어 동점 정렬이 달라졌기 때문으로 추정한다(미확인). `rsf_eval.py`는 재실행하지 않았다.
- 결과 CSV·그림은 ⑯대로 git에 올리지 않는다(`output/`은 `.gitignore`).

### 남은 일
- 인천 서구·중구 입지(중심지 거리) 이상의 원인 확인. 나머지(⑳ 위험 원천 분해, ㉑·㉒ Moran's I, ㉔ 정적 리포트)는 팀원 항목을 따른다.

---

## ㉖ 인터랙티브 웹사이트 (`prep_site_data.py` -> `build_site.py` -> `site/index.html`)

- **구성**: 서버 없는 단일 HTML(약 0.8MB, 데이터 내장, 다크 모드). 탭 4개 —
  (1) **지도 탐색**: 시군구 버블 지도(크기=점포 수, 색=M3L 위험 배수 또는 실제 폐업률), 업종 필터, 휠 확대·드래그, 버블 클릭 시 패널: 위험 배수·실제 폐업률, **"왜" 요인별 배수 막대(SHAP 분해)**,
  규칙 기반 한 줄 해설, BC 연령·성별 구성과 1~6월 소비 추이, 상권 프로필, 모형이 참고하는 이웃 5곳(점선). 질문 입력창("동탄 서양음식 왜 위험해?")은 지역·업종 이름을 찾아 같은 패널을 연다. `?q=동탄 서양음식`, `?area=경기도 화성시 동탄구#area`로 딥링크 가능.
  (2) **상권 분석**: 시군구 프로필(업종별 점포·폐업률·배수·영업연수·프랜차이즈·BC 월평균 소비·점포당 소비), 업종별 위험/안전 상위 10, 업종 내 소비 백분위 vs 폐업률 산점도. (3) **생존 분석**: 리포트 본문 재사용. (4) **방법·한계**.
- **설계 결정**: 점포 단위 점·위험 색칠은 하지 않았다(검증된 것은 그룹 수준이고 점포 단위 C-index는 0.637) / 시군구 경계 대신 좌표 중앙값 버블(2026 개편 지역명과 경계 파일 불일치 회피) /
  "이웃 폐업 전이" 애니메이션·화살표는 인과적 전파로 읽혀 제외하고 "모형이 참고하는 이웃" 점선으로 대체 / LLM 챗봇은 서버·API 키·환각 위험으로 제외하고 규칙 기반 설명(값은 분해 결과에서만 옴)으로 대체.
- **배경 지도(추가)**: 지도를 SVG에서 **Leaflet + OpenStreetMap 타일(회색조 CSS 필터, 다크 모드는 반전)** 로 교체. 시군구 중심점을 pyproj로 EPSG:5174 -> WGS84 변환(오차 수백 m, 시군구 규모에서는 무시)해 위·경도를 `site_data.json`에 넣었고, 제주·울릉 위치 조정은 없앴다.
  Leaflet 1.9.4(BSD-2)는 `vendor/`에 두고 HTML에 인라인한다(CDN 의존 없음). 배경 타일만 외부라 인터넷이 필요하며 실패해도 버블·패널은 동작한다. CARTO 무료 타일은 API 키가 필요해져(화면에 "API KEY REQUIRED") OSM으로 바꿨다 — OSM 타일 정책상 소규모 사용만 허용되므로 트래픽이 커지면 유료/자체 타일로 교체.
- **질문 입력창(규칙 기반)**: 이해하는 형식은 세 가지 — 지역+업종("동탄 서양음식"), 지역만("강남구"), 순위("한식 위험한 곳", "경남 제과점 안전한 곳"; 점포 300개 이상 그룹, 업종 없이 물으면 시군구 2,000개 이상). 시군구는 이름 일부, 시도는 약칭(경남·경북 등) 지원.
  미지원(안내문으로 알림): 7개 외 업종(카페·치킨 등), 두 지역 비교, 시점별 추세, 예측. 입력창 아래에 형식과 예시 버튼을 상시 표시한다.
- **배경 지도 폴백**: OSM 타일 로드가 연속 3회 실패하면 Esri 라이트그레이 타일로 자동 전환, 그것도 실패하면 안내 문구를 띄운다(`?tiles=esri`로 강제 확인 가능). file:// 로 열 때 OSM이 Referer 없는 요청을 막을 수 있어 넣었다.
- **표현 원칙**: BC 연령 구성은 막대에 † 표시와 옅은 색으로 "지역 유형 신호"임을 표시하고, 한 줄 해설의 "위험 요인" 목록과 순위표의 "가장 큰 요인"에서 제외했다(시군구 내 예측 기여 없음, ⑱·⑲·㉓).
- **수치 정정**: 모형 적합 표본은 BC 변수 결측 31개를 뺀 **635,536점포·폐업 25,329건**이다(코호트 원본 635,567/25,330). 리포트 KPI를 이 값으로 바꿨다(build_report.py가 site_data.json에서 읽음).
- **확인 필요**: BC 원자료의 연령 코드(1~6) 정의가 저장소에 없어 화면에는 "연령1~6"으로 표기했다(코드 순서만 사용). 명세를 확인해 라벨을 바꿀 것. 성별 코드는 1=남, 2=여, 3=법인으로 가정.
- **실행**: `python prep_site_data.py`(코호트 로드, 약 1~2분) -> `python build_site.py` -> `site/index.html`을 브라우저로 연다. 배포는 정적 호스팅(GitHub Pages 등)에 이 파일 하나면 된다. 배포는 `docs/index.html`(site/와 동일 내용)을 GitHub Pages(Settings > Pages > Branch main, 폴더 /docs)로 서비스한다. 공모전 규정에 재배포 금지 문구는 없다고 확인했다(저장소 공개 여부·규정 변경은 별도 확인).

---

## ㉗ 상권 유형화와 내 점포 진단 계산기 (`group_types.py`, `prep_site_data.py`, `build_site.py`)

- **상권 유형(`group_types.py`)**: M3L 요인 분해의 4개 요인 log 배수(영업연수·프랜차이즈·점포 특성·자기·이웃 폐업 이력)로 시군구×업종 1,782개 조합을 K-평균(점포 100개 이상으로 학습, 나머지는 최근접 배정)으로 묶었다.
  BC 연령·성별은 지역 유형과 겹쳐 유형이 지역의 복사본이 되므로 제외했다. k=5(실루엣 0.345 — **군집 경계가 뚜렷하지 않고 연속적인 프로필을 편의상 나눈 것**).
  | 유형 | 점포 비중 | 폐업 비중 | 상대 폐업 위험 | 실제 폐업률 | 대표 업종 |
  |---|---|---|---|---|---|
  | A 프랜차이즈 많음·폐업 잦은 지역·신생 점포 많음 | 12.9% | 17.3% | ×1.38 | 5.33% | 서양음식·제과점·스낵 |
  | B 폐업 잦은 지역·개인 점포 중심 | 36.7% | 41.6% | ×1.15 | 4.53% | 한식·일식·스낵 |
  | C 폐업 드문 지역·신생 점포 많음 | 11.1% | 12.0% | ×1.05 | 4.31% | 서양음식·제과점·스낵 |
  | D 프랜차이즈 많음·신생 점포 많음·대형·다중이용 | 5.2% | 4.3% | ×0.89 | 3.29% | 편의점·제과점 |
  | E 폐업 드문 지역·개인 점포 중심·오랜 점포 많음 | 34.1% | 24.8% | ×0.79 | 2.89% | 한식·스낵·일식 |
  위험이 몰린 구간은 A·B(점포의 49.6%, 폐업의 58.9%). 유형 이름은 중심의 요인 배수에서 규칙으로 만든다.
- **사이트 반영**(팀원 버전 `build_site.py`에 추가): 색 기준에 "상권 유형", 유형별 범례, 패널의 "상권 유형" 카드(유형 통계·요인 프로필·우선 지켜볼 지표), 상권 분석 탭의 유형 표("지도에서 보기").
  시군구 단위(업종=전체)에서는 점포 수 기준 최다 유형을 색으로 쓴다. 주소 `?mode=type`으로 공유.
- **내 점포 진단 계산기**: 조합을 고르면 패널에 개업 경과 연수(슬라이더)와 프랜차이즈 여부를 넣어 그 조건 점포의 상대 폐업 위험을 계산한다. Cox 선형예측자가 선형이라 브라우저에서 정확히 계산된다:
  `배수 = 조합 평균 배수 × exp(β_영업연수·(ln(1+연수) − 조합 평균 ln(1+연수)) + β_프랜차이즈·(가맹 − 조합 가맹 비중))`, β는 `output/11_final_coef.csv`(영업연수 −0.264, 프랜차이즈 −0.756). 나머지 점포 특성은 조합 평균으로 둔다.
  표시 문구는 "비슷한 조건 점포들의 평균 대비 상대 위험(통계적 연관), 확정 예측 아님, 점포 단위 C-index 0.64"이다. 프랜차이즈 계수의 HR이 0.47로 크다 — 프랜차이즈 여부는 수작업 브랜드 목록 기반이고 인과가 아니다.
- **실행 순서**: `explain_final.py` → `group_types.py` → `prep_site_data.py`(site_parts/site_data.json도 갱신) → `build_site.py`(또는 산출물이 없으면 `build_site_offline.py`).

---

## ㉘ 전향(시간 밖) 검증 (`forward_validation.py`)

- **설계**: 1/1 코호트(1~6월)로 학습한 모형이 **이후 실제 폐업**을 맞히는가. 학습은 그룹 5-fold(bc_scale_check.py와 같은 분할·적합 캐시 재사용)라 평가 점포는 **시간과 그룹이 모두 학습과 분리**돼 있다.
  평가 코호트는 2026-06-30에 영업 중인 634,490점포를 2026-09-16까지 78일 추적(폐업 10,523건, 1.66%). 특성은 6/30 기준으로 다시 계산(영업연수, 그룹·시군구·이웃 직전 1년 폐업 이력). BC는 1~6월 집계라 6/30에 알려진 정보다.
  **정정**: 앞서 "폐업 기록이 9월 28일까지 있다"고 했으나, LOCALDATA 폐업 기록의 신뢰 구간 끝은 **9월 16일**(그 이후는 4건, cox_rsf.py의 DATA_END)이다. 평가 창은 이 끝에 맞췄다.
- **결과(점포 C-index / 조합 순위상관 Spearman 전체 [95% CI] / 같은 시군구 내)**
  | 모형 | C-index | 전체 | 시군구 내 |
  |---|---|---|---|
  | M2 사업장 | 0.611 | 0.27 [0.21, 0.33] | 0.20 |
  | M2h + 그룹 폐업률 | 0.621 | 0.35 [0.30, 0.42] | 0.24 |
  | M3 + BC 성별·연령 | 0.625 | 0.38 [0.32, 0.44] | 0.25 |
  | **M3L + 자기·이웃 이력** | **0.627** | **0.40 [0.34, 0.46]** | 0.24 |
  | 참고: 1~6월 실제 폐업률 그대로(지속성) | - | 0.32 [0.27, 0.39] | 0.14 |
  | 참고: 직전 1년 폐업률만 | - | 0.29 [0.24, 0.35] | 0.17 |
- **증분(전체 기준 ΔSpearman)**: 그룹 직전 1년 폐업률 +0.086 [0.061, 0.111] **0 제외** / BC 성별·연령 +0.024 [−0.010, 0.053] 0 포함 / 자기·이웃 이력 +0.023 [−0.006, 0.045] 0 포함(⑳의 학습 기간 검증에서는 +0.031로 0 제외였다 — 방향은 같지만 이번에는 확정 못 함) /
  **최종 모형 − 지속성 기준 +0.077 [0.013, 0.139](시군구 내 +0.095 [0.017, 0.177])로 0 제외** — 모형이 "1~6월 폐업률을 그대로 쓰는 단순 방법"보다 낫다.
- **위험 십분위(점포 단위)**: 실제 78일 폐업률이 0.78%(최저) → 3.32%(최고)로 단조 증가, 평균 대비 0.47배 ~ 2.00배(최고/최저 4.3배). 위험 상위 10% 점포가 실제 폐업의 20.0%, 20%가 35.5%, 30%가 48.3%를 차지.
- **해석과 한계**: 판별력은 시간이 지나도 유지된다(C-index 0.637 → 0.627). 조합 순위상관이 학습 기간 검증(0.59)보다 낮은 것은 78일이라 조합별 폐업 수가 적어 관측 폐업률의 잡음이 커진 영향이 크며 직접 비교할 수 없다.
  BC 변수와 이웃 이력의 추가 기여는 이 검증에서 확정되지 않았다 — 핵심 결론(지역·점포 요인이 폐업을 가르고 BC 소비 규모는 추가 설명력이 없다)과 모순되지 않는다. 분할·평가 창이 각 1회.
- **사이트**: '모형 근거' 탭 끝에 전향 검증 카드(십분위 막대, 모형별 순위 일치도 표, 해석)를 추가했다. 카드는 `build_site.py`가 output/13_*.csv에서 만들고 `site_parts/forward.html`에 보관한다(오프라인 빌드 지원).
- 산출: `output/13_forward_summary.csv`, `13_forward_increment.csv`, `13_forward_decile.csv`.

---

## 산출 파일

| 파일 | 생성 스크립트 | 행수 | 용도 |
|---|---|---|---|
| `data/localdata_wide.csv` | `preprocess_localdata.py` | 2,144,806 (37컬럼) | 정제된 전체 컬럼 통합본. EDA 작업대 |
| `data/localdata_clean.csv` | `preprocess_localdata.py` | 2,144,806 (27컬럼) | `SELECTED_COLS`만 선택한 조인/모델용 (EDA 후 갱신). 생존분석 관측단위(사업장 단위, event_observed·is_multiuse·시설총규모·시설총규모_결측여부 포함, 좌표/시설총규모 이상치 정제 완료) |
| `data/bc_clean.csv` | `preprocess_bc.py` | 242,574 | BC카드 정제본(통합업종·공백 정리) |
| `data/final_joined.csv` | `join_datasets.py` | 2,144,806 (42컬럼) | 최종 조인본 — 매칭률 99.99%(미매칭 255행). `localdata_clean`(27컬럼) + BC카드 정적 공변량(bc_amt_total, bc_cnt_total, amt_share_gender_*, amt_share_age_*, bc_amt_trend_slope, bc_amt_cv, bc_amt_growth_ratio) + `in_bc_window` 플래그. 사업장 row 단위 유지 |
| `data/bc_group_month.csv` | `join_datasets.py` | 13,447 (2,244그룹 × 6개월) | (시도, 시군구, 업종, 월) 단위 BC 표(금액·건수·성별/연령 구성비). landmark 강건성 등 시점을 맞춰 붙일 때 사용 |
| `data/region_master.csv`, `data/region_unmatched.csv` | `build_region_master.py` | 256 / 11종 | 지역 키 기준표(ABP 255 + 합성 1)와 표준에 없는 LOCALDATA 라벨 |
| `franchise_brands.csv` | 수동 관리 | 172 (사용 149 + 보류·제외 23) | 프랜차이즈 브랜드 키워드(use=1만 사용) |

※ 세 파일 모두 `.gitignore`(`*.csv`)에 의해 git 추적 대상 아님 — 스크립트만 커밋.

---

## 아직 정리 안 된 것 / 다음 논의 필요

- [ ] `event_observed` — '취소/말소/만료/정지/중지'(생활_대규모점포.csv, 37행) 세부 분석 완료:
      상세영업상태명 확인 결과 128건(필터 전) 전부 `직권취소` 단일 유형이고, 재개업일자가
      있는 건 1건뿐(99.2% 재개업 기록 없음) → censored가 아니라 event(1)로 재분류하는 게 맞음.
      단, `폐업일자`가 비어있어 `인허가취소일자`(그마저 128건 중 85건 결측)를 날짜 소스로
      추가해야 함 — `base_cols`에 `상세영업상태명`, `인허가취소일자` 추가 후 코드 반영 필요.
      전체의 0.0017%라 영향은 미미, 우선순위 낮음. '휴업'은 재개업 기록 거의 없어도(98%
      결측) 현재 censored 유지가 맞음(아직 진행 중인 상태라 정의상 censoring).
- [x] 인천 구 크로스워크 — **"어느 쪽 기준에 맞출지"는 확정**(ABP 기준, 코드 반영 및
      매칭률 99.93% 검증 완료). 남은 건 `INCHEON_CROSSWALK`의 개별 대응관계(검단구→서구,
      서해구→서구, 영종구→중구, 제물포구→미추홀구)가 실제 인천시 고시 기준으로 정확한지
      공식 출처로 한 번 더 대조 확인하는 것뿐 — 방향 재논의 대상 아님.
- [ ] 소상공인시장진흥공단 상가(상권)정보 — 아직 미다운로드, 3단계(Cox/RSF 경쟁밀도 공변량)에서 필요, 지금 진행을 막지는 않음
- [ ] 원본 5개 소스 파일(ABP + LOCALDATA 4종) 체계적 EDA — 다음 단계로 진행 예정
